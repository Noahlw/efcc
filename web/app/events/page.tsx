"use client";

import { AppShell } from "@/lib/app-shell";

export default function EventsPage() {
  return (
    <AppShell>
      <main
        style={{
          maxWidth: 600,
          margin: "2rem auto",
          padding: "0 1rem",
          fontFamily: "sans-serif",
        }}
      >
        <p>聚會 (placeholder)</p>
      </main>
    </AppShell>
  );
}
