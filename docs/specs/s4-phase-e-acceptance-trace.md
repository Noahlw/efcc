# S4 Phase E — Operations Route Acceptance Trace

**Status:** Automated evidence recorded 2026-09-01 on local loopback Worker `http://127.0.0.1:8787` with disposable `E2E_`/`E2E_DEMO_` fixtures. Every numbered automation row is `READY` where cited; manual rows remain `MANUAL — unclaimed`. No real-device, print, or assistive-technology claim is made from headless evidence.

## Scope and provenance

- **Parent:** #475
- **Tickets:** #491 (scanner, Events, and attendance operations), #492 (remaining management operations), and #493 (Identity and Permission management)
- **Grouped PR title:** `feat(s4-e): operations route polish`
- **Phase base branch:** `feat/s4-d-member-public-route-polish`
- **Verified Phase D PR #502 head / Phase E base:** `7547fbf8ee6ed8b441d173135550081a487b1f72`
- **Phase D authority:** normalized identities and Worker/D1 projections from the Phase D stack; browser projections shape affordances but never authorize a mutation.
- **Required sources:** parent #475; issues #491/#492/#493; `docs/specs/s4-phase-d-acceptance-trace.md`; `CONTEXT.md`; the approved S4 Phase E wave plan.
- **Out of scope:** Phase F, production D1, Apps Script, Google Sheets, remote Worker or deployment/promotion, merge, force-push, screenshots, image snapshots, pixel diffs, formal WCAG certification, and unclaimed human evidence.

## Evidence vocabulary

The numbered trace uses exactly these columns: **ID**, **Criterion**, **Precondition / exact action**, **Persona / fixture**, **Observable DOM / HTTP / D1 result**, **Viewport / state**, **Exact test seam**, **Automation owner / status**, and **Manual owner / status**. `PENDING` means the named automation owner has not yet supplied evidence. A row is `READY` only after its automated outcome and every required manual gate are explicitly classified; no manual gate is auto-claimed.

`W7 = 320, 390, 600, 799, 800, 1024, 1440 CSS px`. Both 799 and 800 are mandatory for shell-sensitive states. The management suites additionally retain **900 CSS px** for the 800–1023 interior reflow band. Geometry evidence is numeric CSS-pixel DOM evidence from pinned local Chromium, never a screenshot assertion. Automated DOM/API checks do not claim camera quality, native print preview, keyboard or assistive technology behavior, motion preferences, forced colors, zoom, reflow, or text-spacing evidence.

## Material state matrices

### Scanner, Events, and attendance (#491)

| Surface | Material states | Widths / additional state |
| --- | --- | --- |
| Self and Guest scanner | Self camera-first phone entry, camera stop/failure fallback, deterministic desktop manual entry, Guest no-camera entry, chooser, duplicate/success, resolve and submit retry | W7; 320×568 and 390×667 short-height camera/manual states |
| Assisted and Operator | scoped Event selection, requested Event, forbidden/out-of-scope Event, auth expiry and return, member search/empty/error, roster, operator void, guest correction | W7; material auth, forbidden, error, and roster states |
| Event detail and print | open/closed/cancelled presentation, scanner link, attendance-aware action guard, printable Event roster/sheet | W7; print-media DOM state, with native print inspection manual |

### Remaining management operations (#492)

| Surface | Material states | Widths / additional state |
| --- | --- | --- |
| Management Hub and settings | server-projected groups and entry card, loading, forbidden, empty projection, recoverable retry, safe Back, read-only check-in methods/window and GMT+8 timezone | W7 plus 900 CSS px where the management interior is exercised |
| Home Content | Draft/Published, Template A/B, save, immediate publish, scheduled HK wall-time publish, featured-event preview, phone/desktop preview, audit history, recoverable failure, version conflict/reload, long content | W7; long-content and conflict states at 320/600/799/800/900/1024/1440 |
| Account, Member, and Approval directories | loading/ready/empty/error/forbidden, URL filters/search/cursor, selected detail, Back/return/focus restoration, batch selection/decision, retry and dialog states | W7 plus 600 Sheet, 900 interior reflow, and 1024 list/detail sticky state |

### Identity and Permission management (#493)

| Surface | Material states | Widths / additional state |
| --- | --- | --- |
| Identity Tree and identity detail | canonical list, valid detail, unknown/malformed/archived/unauthorized target fallback, category expansion, create, rename, rescope, sibling reorder, stale order conflict | W7 plus 900 CSS px management interior and 1024/1440 detail states |
| Permission Editor | one selected identity, searchable capability groups, projected locked reasons, Admin all-on lock, dirty/save/busy/success/error, revision conflict and explicit restart, capped Sheet versus dedicated review | W7; material 320/600/799/800/900/1024/1440 states |
| Account Access and legacy entry | eligible Active Account search, assignment/revoke, impact preview, lifecycle history, idempotent/revision conflict, canonical `/permissions` redirect and malformed URL fallback | W7 plus 900 CSS px management interior; legacy redirect at 320/799/800 |

## Acceptance trace rows

### Ticket #491 — scanner, Events, and attendance operations

**Domain invariant:** `useAttendanceFlow` remains the single Self/Guest resolve state machine; Worker attendance handlers remain authoritative for enrollment, Event window, scope, duplicate, void, and correction outcomes. `rememberDeepLink`/`consumeDeepLink` own safe authenticated recovery.

| ID | Criterion | Precondition / exact action | Persona / fixture | Observable DOM / HTTP / D1 result | Viewport / state | Exact test seam | Automation owner / status | Manual owner / status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E-491-01 | Self remains camera-first on phone with manual fallback; desktop remains deterministic manual; Guest remains no-camera. | Open `/scanner` as Self at phone width and allow camera startup; stop scanning or force startup failure, then enter a valid Event Manual Check-In Code. Repeat Self at desktop and choose Guest without enabling camera. | `E2E_DISPOSABLE_MEMBER`, signed-out/explicit Guest fixture, one open `E2E_DEMO_` Event, disposable local Worker/D1. | `ScannerBoundary`/`SelfCheckInPanel` expose `<video>` and the camera action only for phone Self while startup is active; fallback input is available after stop/failure; desktop Self has deterministic manual entry; Guest renders no camera affordance or decoder setup. `resolveAttendance` and `selfCheckIn`/`guestCheckIn` receive the expected public entry and return the existing success/duplicate envelope. | W7 ready; 320×568 and 390×667 short-height camera/manual states; desktop manual at 800/1024/1440. | `ScannerBoundary`, `SelfCheckInPanel`, `AttendancePanel`, `useAttendanceFlow`, and `useQrCamera`; `web/lib/scanner-boundary.test.tsx`, `self-check-in-panel.test.tsx`, `attendance-panel.test.tsx`, `use-qr-camera.test.tsx`; authenticated `tests/e2e/attendance-d1.config.ts`. | Attendance491 / READY — automated — `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/scanner-boundary.test.tsx lib/self-check-in-panel.test.tsx lib/attendance-panel.test.tsx lib/assisted-scanner-panel.test.tsx lib/attendance-operator-panel.test.tsx lib/attendance-roster.test.tsx lib/use-qr-camera.test.tsx lib/programs/event-detail.test.tsx` — 8 files, 103 passed; `PROGRAMS_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/attendance-d1.config.ts` — 52 passed (phone-375x667, desktop-1280x720, disposable local D1/loopback Worker); screenshots not applicable to component seam; manual camera/device gates not claimed | Human reviewer / MANUAL — unclaimed (real camera/video and device behavior) |
| E-491-02 | Assisted/operator auth expiry preserves a safe deep link and returns through sign-in; chooser/roster authority is server-shaped by identity and scope. | As an authorized Assisted/Operator persona, open an Event detail or `/scanner?mode=assisted&event=<id>`, expire the session during Event selection/search/roster action, sign in, and return. Repeat with malformed/stale scanner intent and a valid-looking but forbidden requested Event. | Disposable Admin/Staff/Program Leader projections with exact Program/Department scope; authorized, forbidden, stale, and malformed local Event fixtures. | `rememberDeepLink` preserves same-origin path/query/hash and `consumeDeepLink` restores only the safe scanner/Event target after `AUTH_REQUIRED`; malformed or stale intent falls back to Self. A forbidden Event never renders member search or roster actions. `listScannerEvents`, `listManageableEvents`, `searchAttendanceMembers`, and roster/check-in responses determine chooser, search, and roster DOM; no fixed role or client capability branch authorizes access. | W7; auth-expired, malformed, stale, and forbidden states at 320/799/800 plus desktop operator states. | `ScannerBoundary`, `AssistedScannerPanel`, `AttendanceOperatorPanel`, `EventDetail`, `rememberDeepLink`, `consumeDeepLink`; `scanner-boundary.test.tsx`, `assisted-scanner-panel.test.tsx`, `attendance-operator-panel.test.tsx`, `event-detail.test.tsx`; local `tests/e2e/attendance-d1.config.ts`. | Attendance491 / READY — automated — `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/scanner-boundary.test.tsx lib/assisted-scanner-panel.test.tsx lib/attendance-operator-panel.test.tsx lib/programs/event-detail.test.tsx` — included in 8 files, 103 passed; `PROGRAMS_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/attendance-d1.config.ts` — 52 passed (assisted/operator auth expiry, malformed/stale/forbidden intent, disposable local D1/loopback); manual keyboard/real-device return not claimed | Human reviewer / MANUAL — unclaimed (keyboard/screen-reader recovery and real-device return) |
| E-491-03 | Duplicate/window/cancelled/offline/decode/permission/void/correction outcomes remain complete and recoverable. | Exercise duplicate success, window-not-open, cancelled, not-enrolled, offline resolve, offline submit, decode failure, camera permission denied, camera unsupported/unavailable, operator void, and guest correction. Retry every recoverable branch and verify the retained draft/context. | Disposable enrolled member, not-enrolled member, Guest, authorized operator, cancelled/closed/open Events, existing attendance row, and local network/device-failure stubs. | `AttendanceFlow` transitions through `scan`/`chooser`/`outcome` with the existing `AttendanceOutcome` kinds; `useQrCamera` calls distinct denied/unsupported/unavailable callbacks. `resolveAttendance`, `selfCheckIn`, `guestCheckIn`, `voidAttendance`, and `correctGuestAttendance` preserve existing Problem Details, `requestId`/`X-Request-Id`, and D1 audit/history outcomes. Recoverable retry keeps the valid draft/context, focuses the result/error heading once, and announces once; void/correction changes history without deleting the attendance row. | W7 ready; each outcome at 320/799/800 and operator desktop material states. | `useAttendanceFlow`, `useQrCamera`, `ScannerStatusOutput`, `ScannerOutcome`, `AttendanceOperatorPanel`, `AttendanceRoster`; `attendance-panel.test.tsx`, `assisted-scanner-panel.test.tsx`, `attendance-operator-panel.test.tsx`, `attendance-roster.test.tsx`, `use-qr-camera.test.tsx`; `web/lib/attendance-worker.test.ts`, `attendance.test.ts`, `attendance-entry.test.ts`, and `tests/e2e/attendance-d1.config.ts`. | Attendance491 / READY — automated — `pnpm --dir web exec vitest run --config vitest.config.ts lib/attendance-worker.test.ts lib/attendance.test.ts lib/attendance-entry.test.ts` — 3 files, 59 passed; `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/attendance-panel.test.tsx lib/assisted-scanner-panel.test.tsx lib/attendance-operator-panel.test.tsx lib/attendance-roster.test.tsx lib/use-qr-camera.test.tsx` — included in 8 files, 103 passed; local Worker/domain disposable fixtures; manual hardware/assistive announcements not claimed | Human reviewer / MANUAL — unclaimed (camera hardware, assistive announcements, offline device behavior) |
| E-491-04 | Native camera/video/radio/device and print exceptions are documented; equivalent raw controls and attendance CSS Modules are removed. | Exercise `ScannerChooser`/Event picker selection, camera start/stop, native device callbacks, and roster print-media state after all attendance callers are migrated; search shipped TypeScript for obsolete attendance CSS imports and equivalent raw controls. | Clean shipped/test TypeScript tree, native-capable browser stubs, disposable local attendance fixtures; `/prototype` and historical evidence excluded. | `ScannerChooser`/`ScannerEventPicker` retain a `fieldset`/`legend` and native radios with no preselection or implicit submission; `<video>`, MediaStream/device APIs, native `window.print()`, and platform exceptions remain documented. Existing Button/Input/Card primitives own equivalent controls; no executable caller imports `web/lib/attendance-panel.module.css`, and no duplicate scanner lifecycle or fallback implementation remains. | W7 ready; chooser and print-media states at 320/799/800/1024/1440. | `ScannerChooser`, `ScannerEventPicker`, `ScannerCamera`, `CameraFirstScanner`, `useQrCamera`, `AttendanceRoster`; component suite above plus a repository import search and `tests/e2e/phase-e-attendance-geometry.test.ts` print-media project. | Attendance491 / READY — automated — repository TypeScript search — 0 executable imports of `attendance-panel.module.css`; `ScannerChooser`/`ScannerEventPicker` retain fieldset/legend native radios; `<video>`/MediaStream/`window.print()` remain documented platform exceptions; `PHASE_E_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/phase-e-attendance-geometry.config.ts` — 25 passed, 7 intentional skips (print-only project) with print-media DOM verified; manual native camera/video/print preview not claimed | Human reviewer / MANUAL — unclaimed (native camera/video/device operation and native print preview/paper output) |
| E-491-05 | W7 plus short-height/safe-area/799–800/print geometry and required real-device human gates pass. | Reset disposable local D1, run the attendance journeys, and run the dedicated geometry projects with screenshots disabled; inspect critical anchors in Self, Guest, Assisted, Operator, Event detail, and print-media DOM states. | Disposable local D1 and loopback Worker only; `E2E_DISPOSABLE_MEMBER`, Guest, and scoped operator fixtures from the preceding rows. | Numeric DOM evidence proves no horizontal overflow, app-facing targets at least 44×44 CSS px, safe-area/dock clearance, final/error/retry anchor reachability, intentional 799/800 shell transition, short-height camera/manual containment, long CJK/unbroken content containment, and roster visibility under `@media print`; no screenshot assertion and no hardware claim. | W7; 320×568 and 390×667; material 320/799/800 print states; 1024/1440 operator states. | New `tests/e2e/phase-e-attendance-geometry.config.ts` and `phase-e-attendance-geometry.test.ts`, using local `attendance-d1.config.ts` setup and numeric DOM measurements. | Attendance491 / READY — automated — `PHASE_E_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/phase-e-attendance-geometry.config.ts` — 25 passed, 7 intentional skips (print-only skipped on non-print projects; print-media executed), numeric DOM at W7 (320, 390, 600, 799, 800, 1024, 1440) plus 320×568/390×667 short-height, 799/800 shell transition, no overflow, ≥44×44 targets, safe-area/dock, final-anchor, long CJK containment, screenshots off, disposable local D1/loopback; `PROGRAMS_TARGET_URL=.../attendance-d1.config.ts` — 52 passed; manual real-device/print not claimed | Human reviewer / MANUAL — unclaimed (real iOS/Android camera, safe area/dock, and native print inspection) |

### Ticket #492 — remaining management operations

**Domain invariant:** Management Hub grouping, Home Content semantics, directory queries, approval decisions, and settings values remain server/projected-domain contracts. Shared headers, action surfaces, filter sheets, and directory placement change presentation only.

| ID | Criterion | Precondition / exact action | Persona / fixture | Observable DOM / HTTP / D1 result | Viewport / state | Exact test seam | Automation owner / status | Manual owner / status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E-492-01 | Management Hub and settings preserve server-projected grouping, safe Back behavior, and static setting semantics. | Open `/management` for each management-capable projection; exercise loading, forbidden, empty projection, recoverable retry, every projected group/entry, Settings, Check-in Settings, Timezone Settings, and Back from each detail. | Disposable normalized Admin/Staff/Department Manager/Program Leader projections plus forbidden/empty local Worker responses. | `getManagementHub` is the sole grouping/visibility response and `ManagementHubView.groups`/`entryCard` order is rendered without client capability or fixed-role branches. `ManagementHub`, `SettingsHub`, `CheckinSettings`, and `TimezoneSettings` expose safe Back targets (`/management`, then `/management?module=settings`), informational check-in methods/windows and GMT+8 rows, no editable input or mutation, and one focused state heading per retry/error. | W7; loading/forbidden/empty/retry and settings states at 320/799/800/1024/1440; retain 900 CSS px for management interior reflow. | `ManagementHub`, `ManagementHubView`, `getManagementHub`, `useAsyncResource`, `SettingsHub`, `CheckinSettings`, `TimezoneSettings`; `web/lib/management-hub.test.tsx`, `web/lib/settings-hub.test.tsx`, and `tests/e2e/s4-management-hardening.test.ts`. | HubSettings492 / READY — automated — `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/management-hub.test.tsx lib/settings-hub.test.tsx lib/directory-frame.test.tsx lib/management-action-framework.test.tsx` — included in 9 files, 121 passed; `PROGRAMS_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/s4-management-hardening.config.ts` — 136 tests, 62 passed, 74 intentional skips (project/fixture gating), W7 plus 900, disposable local D1/loopback, screenshots off; manual keyboard/focus/AT not claimed | Human reviewer / MANUAL — unclaimed (keyboard/screen-reader focus, reduced motion, forced colors, and touch Back behavior) |
| E-492-02 | Home Content preserves Draft/Published, save/publish, preview, audit, conflict, and long-content behavior while adopting shared management chrome. | Load Home Content; edit Template A and B; save a draft; publish immediately and on a scheduled Hong Kong wall-time; open featured-event, phone, and desktop previews; inspect audit history; force recoverable failure and version conflict, then use the explicit reload path without losing the applicable draft. | Disposable Admin/Staff Home Content projections, Draft and Published versions, featured Event preview fixture, HK wall-time values, and local conflict/non-conflict responses. | `getHomeContent`, `saveHomeDraft`, `publishHomeContent`, `listHomeAudit`, and `getFeaturedEventPreview` keep their existing payload/version semantics. `HomeContentEditor` visibly distinguishes Draft/Published, preserves a non-conflict draft on failure, shows the authoritative latest version on conflict with explicit reload, retains immediate/scheduled mode and HK conversion, and renders long content without clipping while `ManagementPageHeader`/`ActionSurface` own the shared chrome. | W7; long content, preview, save/publish, and conflict states at 320/600/799/800/900/1024/1440. | `HomeContentEditor` and `web/lib/home-cms-api.ts` public functions; `web/app/management/home-cms-editor.test.tsx`, `home-cms-editor.test.tsx`, and the Home Content path in `tests/e2e/s4-management-hardening.test.ts`. | HomeContent492 / READY — automated — `pnpm --dir web exec vitest run --config vitest.components.config.ts app/management/home-cms-editor.test.tsx` — included in 9 files, 121 passed; `PROGRAMS_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/s4-management-hardening.config.ts` — Home Content path 62 passed/74 skips (Draft/Published, HK wall-time, preview, audit, conflict reload, long content, W7/600/799/800/900/1024/1440, screenshots off, disposable local D1/loopback); manual AT/motion/color not claimed | Human reviewer / MANUAL — unclaimed (keyboard/screen-reader editor review, reduced motion, forced colors, zoom/reflow, and text-spacing) |
| E-492-03 | Remaining Account/Member/Approval callers use Directory/Action modules and no equivalent raw control remains. | Search/filter Account and Member directories, change URL filters, select a row, open detail, use Back/validated `return`, load more/retry, select approval requests across list/detail, and complete or recover a batch decision. | Disposable local Account/Member/Registration Request records; normalized Admin/Staff/Department Manager/Program Leader scopes; empty, forbidden, stale, and recoverable-error fixtures. | `AccountDirectoryPanel`, `MemberDirectoryPanel`, `ApprovalQueue`, and `ApprovalDetail` retain domain query/filter/selection/decision semantics and server authority. `DirectoryFrame` coordinates loading/ready/empty/error/forbidden, selected-row focus restoration, `ManagementPageHeader` safe Back, `ManagementFilterSheet` filters, and `ActionSurface` actions; HTTP responses and D1 decisions remain existing envelopes/audit outcomes. | W7; 600px filter Sheet, 799/800 shell transition, 900 interior reflow, 1024 list/detail sticky layout, and 1440 desktop. | `DirectoryFrame`, `ManagementPageHeader`, `ManagementFilterSheet`, `ActionSurface`, `AccountDirectoryPanel`, `MemberDirectoryPanel`, `ApprovalQueue`, `ApprovalDetail`; their focused component tests and `tests/e2e/s4-management-hardening.test.ts`. | Directories492 / READY — automated — `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/account-directory-panel.test.tsx lib/member-directory-panel.test.tsx lib/approval-queue.test.tsx lib/approval-detail.test.tsx lib/directory-frame.test.tsx` — included in 9 files, 121 passed; `PROGRAMS_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/s4-management-hardening.config.ts` — directory/approval path 62 passed/74 skips (W7, 600 Sheet, 799/800, 900 interior, 1024 sticky, screenshots off, disposable local D1/loopback); manual keyboard/dialog AT not claimed | Human reviewer / MANUAL — unclaimed (keyboard/screen-reader selection and dialog focus, reduced motion, forced colors, zoom/reflow, and text-spacing) |
| E-492-04 | Every touched route closes its Must Fix/Covered audit findings and deletes obsolete CSS/modules/tests in its scope. | After all #492 callers are migrated, search production and test TypeScript for route CSS imports, duplicate lifecycle/action/directory implementations, obsolete controls, and stale implementation-shaped tests; inspect the shared-module caller inventory. | Clean shipped/test TypeScript tree, excluding `/prototype` and historical evidence; no remote data. | No executable imports remain for `management-hub.module.css`, `home-cms-editor.module.css`, `approval-queue.module.css`, or `approval-detail.module.css`; equivalent raw controls use the approved local primitives, one lifecycle/action/directory composition owns each state, and removed implementation-shaped tests are not replaced by a second framework. | W7 ready; route/state inventory exercised at 320/600/799/800/900/1024/1440. | Repository search plus `management-hub.test.tsx`, `settings-hub.test.tsx`, `home-cms-editor.test.tsx`, `account-directory-panel.test.tsx`, `member-directory-panel.test.tsx`, `approval-queue.test.tsx`, `approval-detail.test.tsx`, `directory-frame.test.tsx`, and `management-action-framework.test.tsx`. | IntegrationOwner / READY — automated — repository TypeScript search — 0 executable imports of `management-hub.module.css`, `home-cms-editor.module.css`, `approval-queue.module.css`, `approval-detail.module.css`; one lifecycle/action/directory composition; `pnpm typecheck` PASS, `pnpm --dir web typecheck` PASS, `pnpm --dir web build` PASS, `pnpm verify:precommit` PASS (38 prototype, 98 identity, 569 enabled Worker (37 files), 787 component), `git diff --check` PASS; manual whole-route AT/motion not claimed | Human reviewer / MANUAL — unclaimed (whole-route assistive, motion, forced-colors, reflow, text-spacing, and touch review) |
| E-492-05 | W7 plus 600 Sheet, 799/800, 800–1023, 1024 sticky/detail, and long-content geometry passes. | Reset disposable local D1, run the authenticated management journeys with screenshots disabled, and measure Hub/settings, Home Content, directories, approvals, actions, filters, selected details, and long-copy states at every material width. | Disposable local D1 and loopback Worker only; Admin, Staff, Department Manager, Program Leader, Member, and Custom Identity projections where a route is projected to them. | Numeric DOM evidence proves no horizontal overflow, targets at least 44×44 CSS px, 600px Sheet and 800px filter transitions are intentional, 900px interior reflow remains usable, 1024px list/detail sticky placement is reachable, in-flow phone actions clear the dock/safe area, long CJK/unbroken content is contained, and final/error/retry anchors are reachable; screenshots are off. | W7 plus 900 CSS px for the 800–1023 management band; Home/directories/approvals at 320/600/799/800/900/1024/1440. | `tests/e2e/s4-management-hardening.config.ts` and `s4-management-hardening.test.ts` with explicit `w-320`, `w-390`, `w-600`, `w-799`, `w-800`, `w-900`, `w-1024`, and `w-1440` projects. | IntegrationOwner / READY — automated — `PROGRAMS_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/s4-management-hardening.config.ts` — 136 tests, 62 passed, 74 intentional skips (project/fixture gating, 0 failed), numeric DOM at W7 (320,390,600,799,800,1024,1440) plus 900 interior (800–1023), 600 Sheet, 800 filter, 1024 sticky, long-content containment, ≥44×44, no overflow, screenshots off, disposable local D1/loopback; supplemental `pnpm verify` 93 responsive, 28 shell geometry passed; manual real touch/AT not claimed | Human reviewer / MANUAL — unclaimed (real touch hardware, keyboard/screen-reader, reduced motion, forced colors, zoom/reflow, and text-spacing) |

### Ticket #493 — Identity and Permission management

**Domain invariant:** identity hierarchy, permission grants, account assignment, lifecycle impact, and protected-target decisions are projected and authorized by Worker/D1 identity APIs. Browser role labels, hidden navigation, and fixed-role interaction models never become authority.

| ID | Criterion | Precondition / exact action | Persona / fixture | Observable DOM / HTTP / D1 result | Viewport / state | Exact test seam | Automation owner / status | Manual owner / status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E-493-01 | Identity Tree → Detail → Permission Editor and Identity Detail/Account Detail assignment entry points share one server-owned model. | Open Management Roles, select a valid identity, move to detail and Permission Editor, refresh and go Back; open the same assignment flow from Identity Detail and Account Detail while preserving only validated `role`, `view`, `account`, `roleDefinition`, `scopeKind`, `scopeId`, and `return` state. | Disposable normalized Admin/Staff/Department Manager/Program Leader/Member/Custom Identity projections; valid selected identity and authorized Account Detail fixtures. | `loadRoleHierarchy`, `loadRoleDefinitionDetail`, and `loadAccountAccess` responses are the only selected identity/access sources. `RoleHierarchyPanel`, `PermissionEditorPanel`, and `AccountAccessPanel` converge on the same identity/account projection; valid refresh/Back/share links retain safe state, while unknown, malformed, archived, or unauthorized targets fall back to the canonical roles list without a dangerous mutation view. | W7; list/detail and assignment entry states at 320/600/799/800/1024/1440, with 900 CSS px management interior. | `RoleHierarchyPanel`, `PermissionEditorPanel`, `AccountAccessPanel`; `web/lib/identity/role-hierarchy-panel.test.tsx`, `permission-editor-panel.test.tsx`, `account-access-panel.test.tsx`, identity domain tests, and the identity path in `tests/e2e/s4-management-hardening.test.ts`. | IdentityTree493 / READY — automated — `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/identity/role-hierarchy-panel.test.tsx lib/permission-editor-panel.test.tsx lib/account-access-panel.test.tsx` — 3 files, 78 passed; `pnpm verify:identity` — 4 files, 98 passed; `PROGRAMS_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/s4-management-hardening.config.ts` — identity path 62 passed/74 skips (W7 plus 900, screenshots off, disposable local D1/loopback); manual keyboard/navigation not claimed | Human reviewer / MANUAL — unclaimed (keyboard/screen-reader tree/editor navigation and focus restoration) |
| E-493-02 | Admin, Staff, Department Manager, Program Leader, Member, and Custom Identity affordances match exact position, grant, protected-target, and scope rules. | Sign in as each disposable persona; inspect projected categories/actions/locks and attempt in-scope, out-of-scope, protected Admin/會友基礎, archived, self, and tampered scope/account requests through the visible affordances and direct local HTTP calls. | `E2E_DISPOSABLE_` normalized identity fixtures for all six personas, including scoped Department/Program identities and protected targets; disposable local D1. | DOM affordances and explanatory lock reasons equal the returned `RoleHierarchyView`, `RoleDefinitionDetailView`, and `AccountAccessView` projections. Unauthorized or out-of-scope requests return the existing Problem Details/HTTP denial and leave D1 unchanged; no `accounts.role` shortcut, fixed Manager/Leader allowlist, hidden-navigation authority, or client capability recomputation appears. | W7; persona/protected-target states at 320/600/799/800/1024/1440, plus 900 CSS px management interior. | `loadRoleHierarchy`, `loadRoleDefinitionDetail`, `loadAccountAccess`, `RoleHierarchyPanel`, `PermissionEditorPanel`, `AccountAccessPanel`; identity handler/domain tests and `tests/e2e/s4-management-hardening.test.ts` persona matrix. | IntegrationOwner / READY — automated — `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/identity/role-hierarchy-panel.test.tsx lib/permission-editor-panel.test.tsx lib/account-access-panel.test.tsx` — 3 files, 78 passed; `pnpm verify:identity` — 4 files, 98 passed; `pnpm test:workerd` — 37 files, 569 passed (four Phase D excluded files remain excluded by checked-in script); `PROGRAMS_TARGET_URL=.../s4-management-hardening.config.ts` — 62 passed/74 skips persona matrix (Admin/Staff/Department Manager/Program Leader/Member/Custom Identity, protected Admin/會友基礎, screenshots off); manual AT/target-order not claimed | Human reviewer / MANUAL — unclaimed (assistive technology, keyboard target order, reduced motion, forced colors, zoom/reflow, and text-spacing) |
| E-493-03 | Fixed categories, Admin, 會友基礎, safe URL state, create/rescope/reorder/conflict, permission review, assignment impact, and lifecycle states are complete. | Create a scoped and global custom identity; rename/rescope it; reorder siblings with keyboard alternatives and force an order conflict; toggle permission grants and save with three or fewer non-high-risk changes, then with more than three or any `HIGH_RISK_KEYS` change; add/revoke assignments and preview archive/restore impact under stale revision and idempotency retries. | Disposable role hierarchy with protected Admin/highest and 會友基礎/lowest anchors, custom identities, searchable capabilities, eligible Active Account, archived identity, and conflicting revision fixtures. | `loadRoleHierarchy`/`createRoleDefinition`/`renameRoleDefinition`/`rescopeRoleDefinition`/`reorderRoleDefinitions` return authoritative categories, scope, order, and conflict choices (`保留我的排序`/`採用最新排序`). `updateRoleDefinitionGrants` uses a capped Sheet for at most three non-high-risk changes and a dedicated review for more than three or any high-risk key; non-conflict failure retains the draft, `ROLE_POLICY_CONFLICT` offers explicit restart, and returned revision is authoritative. `loadAccountAccess`, `mutateAccountAssignments`, `getRoleDefinitionLifecyclePreview`, and `mutateRoleDefinitionLifecycle` show lost/retained Global, Department, and Program access with supplying identities and preserve atomic/idempotent D1/audit outcomes. | W7; mutation/review/impact states at 320/600/799/800/900/1024/1440. | `web/lib/identity/role-hierarchy.ts` public loaders/mutations, `permission-editor.ts` public loaders/mutation, `account-access.ts` public loaders/mutations/lifecycle preview; `role-hierarchy-panel.test.tsx`, `permission-editor-panel.test.tsx`, `account-access-panel.test.tsx`, `role-handlers.test.ts`, `permission-editor-handlers.test.ts`, `account-access-handlers.test.ts`, `account-access-history.test.ts`, and the local management E2E seam. | IntegrationOwner / READY — automated — `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/identity/role-hierarchy-panel.test.tsx lib/permission-editor-panel.test.tsx lib/account-access-panel.test.tsx` — 3 files, 78 passed; `pnpm verify:identity` — 4 files, 98 passed; `pnpm test:role-hierarchy-geometry` — 49 passed (supplemental numeric, not authenticated READY); `PROGRAMS_TARGET_URL=.../s4-management-hardening.config.ts` — 62 passed/74 skips (create/rescope/reorder/conflict `保留我的排序`/`採用最新排序`, permission review Sheet vs dialog, assignment impact, lifecycle, disposable local D1/loopback, screenshots off); manual Switch/dialog AT not claimed | Human reviewer / MANUAL — unclaimed (keyboard/screen-reader reorder, Switch/review dialog, motion, forced colors, zoom/reflow, and text-spacing) |
| E-493-04 | Legacy permissions redirects land safely on the canonical target; the fixed-role panel and its styling/tests are deleted. | Visit `/permissions` with no return, a valid management return, unsafe/protocol-relative/other-app return, and malformed `role`/`view` state; search source and test TypeScript for the removed fixed-role panel, styling, exports, and implementation-shaped references. | Local authenticated and signed-out loopback sessions; validated and unsafe return values; clean shipped/test source tree excluding historical evidence. | `safeManagementReturnHref` and `/permissions` use `router.replace` to reach `/management?module=permissions`, preserving only a validated management return; unsafe input falls back to `/management`. Malformed or unknown identity state reaches the canonical roles list, not a mutation view. No executable fixed-role panel, style, export, or stale test remains. | 320/799/800 redirect and fallback states; W7 canonical management list state. | `web/app/permissions/page.tsx`, `safeManagementReturnHref`, `ManagementModule`, `RoleHierarchyPanel`, `PermissionEditorPanel`; `web/lib/management-route-redirects.test.ts`, identity panel tests, `tests/e2e/s4-management-hardening.test.ts`, and repository search. | IdentityTree493 / READY — automated — `web/lib/management-route-redirects.test.ts` included in identity/component suites; `PROGRAMS_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/s4-management-hardening.config.ts` — legacy `/permissions` redirect path 62 passed/74 skips (validated/unsafe return, `router.replace` to `/management?module=permissions`); repository search — 0 executable fixed-role panel/style/export; `pnpm typecheck`/`pnpm --dir web typecheck`/`pnpm --dir web build` PASS; manual redirect/focus AT not claimed | Human reviewer / MANUAL — unclaimed (keyboard/screen-reader redirect/focus, reduced motion, forced colors, zoom/reflow, and text-spacing) |
| E-493-05 | Canonical ready W7 and material 320/600/799/800/1024/1440 identity states pass numeric geometry and accessibility seams. | Reset disposable local D1, run the authenticated Identity Tree → Detail → Permission Editor → Account Access journeys with screenshots disabled, and measure list/detail, Sheet/dialog review, action, impact, conflict, and final anchors. | Disposable local Worker/D1 and normalized identity fixtures from the preceding rows; no remote Worker. | Numeric DOM evidence proves no horizontal overflow, app-facing targets at least 44×44 CSS px, long identity/capability/scope labels remain contained, list/detail and 600px Sheet/800px filter transitions are intentional, 900px management reflow is usable, protected/conflict/restart/final anchors are reachable, and action surfaces clear the dock/safe area; no screenshot or automated accessibility claim. | W7 plus 900 CSS px management band; material identity states at 320/600/799/800/1024/1440. | `tests/e2e/s4-management-hardening.config.ts`/`s4-management-hardening.test.ts`; supplemental `tests/e2e/role-hierarchy-geometry.config.ts`/`role-hierarchy-geometry.test.ts` (numeric only, not authenticated READY evidence). | IntegrationOwner / READY — automated — `PROGRAMS_TARGET_URL=http://127.0.0.1:8787 pnpm exec playwright test --config=tests/e2e/s4-management-hardening.config.ts` — 136 tests, 62 passed, 74 intentional skips, 0 failed, numeric DOM at W7 (320,390,600,799,800,1024,1440) plus 900 management band (600 Sheet/800 filter, long labels, anchors, dock clearance, screenshots off, disposable local D1/loopback); supplemental `pnpm test:role-hierarchy-geometry` — 49 passed (numeric only); final smoke `PROGRAMS_TARGET_URL=.../attendance-d1.config.ts && .../s4-management-hardening.config.ts` — 52 passed / 62 passed+74 skips; manual real touch/AT not claimed | Human reviewer / MANUAL — unclaimed (keyboard/screen-reader, reduced motion, forced colors, real touch hardware, zoom/reflow, and text-spacing) |

## Cross-row transport and authority invariants

- Server projections and existing Worker/domain outcomes remain authoritative. UI code renders projected actions, lock reasons, statuses, and explanations; it never branches on a fixed role, `accounts.role`, hidden navigation, browser-derived capability, or a Manager/Leader shortcut.
- Safe return and deep-link handling is same-origin and path/query/hash preserving only for validated destinations. Malformed, duplicate, stale, forbidden, or unrecognized targets fail closed to the canonical safe list/Self destination and never expose a member search, roster, identity mutation view, credential, or secret.
- Cookie/session auth, `{ requestId, data }` success envelopes, `X-Request-Id`, Problem Details failures, actor-bound idempotency, revision/conflict outcomes, audit history, and D1 atomicity remain unchanged at every exercised public module/Worker seam.
- All fixtures and D1 assertions are disposable local `E2E_`/`E2E_DEMO_` data only. Every browser/API row targets the loopback Worker; no remote Worker, production database, Apps Script, or Google Sheets write is evidence.
- Geometry rows use numeric DOM measurements from pinned local Chromium with screenshots disabled. Print-media DOM visibility is automated evidence; native print preview/paper, real camera/video/device behavior, and keyboard/assistive/motion/color/reflow/text-spacing behavior are human evidence only.
- Existing public modules remain the seams: `useAttendanceFlow`, `useQrCamera`, attendance handlers, `ManagementHubView`/`getManagementHub`, `HomeContentEditor`/`home-cms-api`, `DirectoryFrame`/`ManagementPageHeader`/`ActionSurface`/`ManagementFilterSheet`, and identity hierarchy/permission/account-access APIs. No second scanner, lifecycle, directory, action, form, authorization, or compatibility layer is introduced.

## Manual-only rows

Automated evidence must not claim these gates:

| ID | Required human evidence | Manual owner / status |
| --- | --- | --- |
| E-491-M1 | Real iOS/Android camera/video review for phone Self camera-first startup, permission denied/unsupported/unavailable wording, stop/retry/track-ended cleanup, decoder quality, Guest no-camera behavior, safe-area/dock clearance, and touch reachability. | Human reviewer / MANUAL — unclaimed |
| E-491-M2 | Native print inspection of the Event roster/check-in sheet in the system print flow and on paper/PDF output, including print-only visibility, clipping, page breaks, and device/browser parity. | Human reviewer / MANUAL — unclaimed |
| E-492-M1 | Keyboard-only and VoiceOver/NVDA review of Hub/settings, Home Content, Account/Member directories, and Approvals: logical order, visible focus, Sheet/dialog focus return, live errors, target size, Back/return, and selected-row restoration. | Human reviewer / MANUAL — unclaimed |
| E-492-M2 | Reduced-motion, forced-colors, zoom/reflow, text-spacing, real touch hardware, safe-area/dock, and related assistive-device review across the W7/900/1024 management states. | Human reviewer / MANUAL — unclaimed |
| E-493-M1 | Keyboard-only and VoiceOver/NVDA review of Identity Tree, detail, reorder controls, Permission Editor Switch/review surfaces, Account Access impact/lifecycle dialogs, protected-target messaging, and focus restoration. | Human reviewer / MANUAL — unclaimed |
| E-493-M2 | Reduced-motion, forced-colors, zoom/reflow, text-spacing, real touch hardware, safe-area/dock, and related assistive-device review for identity list/detail/editor/access states at W7 and the 900px management band. | Human reviewer / MANUAL — unclaimed |
