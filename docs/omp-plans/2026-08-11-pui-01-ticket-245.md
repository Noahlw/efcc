# PUI-01 (Ticket #245) — Capability-Aware Programs Entry Acceptance Trace

**Date:** 2026-08-11
**Worktree:** `feat/foundation-rebuild` (isolated linked worktree from `origin/main` at `c7514a2`)
**Spec authority:** Issues #241, #242, #245; ADR-0017, ADR-0018, ADR-0019, ADR-0020, ADR-0025, ADR-0026
**Scope:** Participant-default Programs entry, stable shell integration, capability-aware Management entry, URL/auth restoration. Directory, Program detail, Enrollment, Management Workspace, Scanner, and notification UI are explicitly deferred.

> This trace is the authoritative implementation and review record for PUI-01. The first version was recorded after the initial implementation commit; that historical process deviation is noted below rather than being presented as a pre-implementation gate. Assertions observe rendered DOM, URL state, or same-origin Worker response state. Browser authorization consumes server-shaped effective access/scope; role-name branching is forbidden.

## Acceptance criteria

| # | Criterion | Assertion method |
|---|---|---|
| PUI-01 | Member, Program Leader, Department Manager, Staff, and Admin enter `/programs` in Participant mode by default | Component tests with five server-shaped bootstrap fixtures; initial DOM exposes Participant mode and no management workspace/data |
| PUI-02 | Authenticated shell keeps Home/Programs/Events/Scanner/Account destinations and incumbent visual language at phone and larger viewports | Component/DOM tests for stable nav and responsive smoke at representative narrow and desktop widths; visual screenshots inspected against `DESIGN.md`/`globals.css` |
| PUI-03 | Compact Management entry is present only when server-projected effective scoped capability includes a relevant management capability | Capability projection tests with capability-bearing and empty scopes; no test or implementation branches on `profile.role`; loading frame shows no unauthorized management data |
| PUI-04 | Participant/Management switching is keyboard and screen-reader operable and preserves a selected valid destination | Interaction tests using accessible roles/names, keyboard activation, `aria-current`/live status, and URL assertions for mode/destination |
| PUI-05 | Refresh, Back, direct Program intent, expired-session recovery, and post-login restoration are deterministic and URL-addressable | App-shell/session component tests with query/path intent fixtures; malformed/unauthorized intents fall back safely within Programs or show forbidden state; expiry stores/restores safe intent |
| PUI-06 | Loading, forbidden, no-management-scope, malformed intent, and recoverable transport states stay in Programs task | Component tests assert state-specific headings/messages/actions and retry behavior without navigating to unrelated sections |
| PUI-07 | Browser consumes server-shaped access/scope and server remains authoritative | Bootstrap/API contract tests assert effective access is transported from `/api/v1/auth/me` or the approved Programs boundary; direct unauthorized Worker request remains denied; no role-name authorization code in changed UI |

## Test seams

- `Bootstrap`/auth-me response projection and `buildBootstrap`/`restoreBootstrap` URL handoff.
- `/programs` entry component observable DOM, accessible controls, and `usePathname`/router URL transitions.
- Existing `NavBar`/`AppShell` integration at the rendered shell boundary.
- Same-origin Worker contract only where the implementation changes the capability projection endpoint or response shape.
- Existing responsive Playwright config for the live shell; no new authenticated E2E pipeline is created without operator approval.

## Verification record

Append exact commands, pass counts, screenshots/trace paths, review findings, and any deferred evidence here after implementation. The local `wrangler dev` + D1 browser gate is required for `READY` under ADR-0029; an optional deployed smoke is operational evidence only.

## Current post-review verification

- Current implementation head: `feat/foundation-rebuild` after `origin/main` `c7514a2`.
- `fnm exec --using=v22.22.3 pnpm run verify` — passed: root typecheck, 278 root tests, web typecheck, 293 web tests, 206 component tests, and responsive shell (38 passed, 1 viewport-inapplicable skip).
- `fnm exec --using=v22.22.3 pnpm --dir web exec vitest run --config vitest.components.config.ts lib/programs/programs-boundary.test.tsx` — 22/22 passed, including keyboard mode entry/return, duplicate intent rejection, and error-state focus.
- `fnm exec --using=v22.22.3 pnpm --dir web exec vitest run --config vitest.config.ts lib/programs/programs.test.ts --pool=forks --maxWorkers=1` — 90/90 passed, including the capability-only `/api/v1/programs/access` Worker contract and Program Leader scope projection.
- `PROGRAMS_TARGET_URL=http://127.0.0.1:8788 fnm exec --using=v22.22.3 pnpm exec playwright test --config=tests/e2e/programs-d1.config.ts` — 12/12 passed at phone and desktop sizes against a fresh local Worker + local D1. Port 8788 was used because a stale Worker from this same worktree occupied the documented default 8787; no other process was terminated.
- The participant entry now requests only the capability-only access projection; it does not download full Program DTOs or check-in tokens merely to decide whether to show Management.
- Review findings addressed: full DTO capability probe, participant-only single-tab semantics, duplicate query ambiguity, recoverable-state focus, keyboard operation, real Back/Forward/refresh coverage, and expired-session post-login restoration.
- Review finding retained as a process note: the acceptance trace was not created before the first implementation commit, so that historical gate cannot be retroactively satisfied. It is now stated accurately; no production deployment claim is made.


- `pnpm --dir web typecheck && pnpm typecheck` — passed web application, Worker, root TypeScript, and E2E TypeScript checks.
- `pnpm --dir web test` — 14 files, 292 tests passed.
- `pnpm --dir web test:components -- --pool=forks --maxWorkers=1` — 17 files, 204 tests passed.
- `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/app.test.tsx lib/programs/programs-boundary.test.tsx lib/session-deep-link.test.tsx --pool=forks --maxWorkers=1` — 3 files, 94 tests passed. The shell regression supplies explicit server-shaped sections and navigation, proves an Admin profile consumes a Member section projection verbatim, and proves missing server projections fail closed; the Programs slice covers same-route query/hash/Back focus, malformed/retry focus recovery with a tab-free accessible region, participant transport errors that remain retryable when the selected Participant tab is activated, default participant FORBIDDEN rendered as a Programs-local forbidden StatePanel whose retry revalidates without `router.replace("/")`,…
- `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/programs/programs-boundary.test.tsx lib/session-deep-link.test.tsx --pool=forks --maxWorkers=1` — 2 files, 23 tests passed.
- `pnpm --dir web build` — passed static Next build; `/home` and `/programs` prerendered with their route-level output.
- Historical implementation note: replaced the obsolete PRG-05 nested-manager `tests/e2e/programs-d1.test.ts` flows with five bounded PUI-01 browser assertions using the existing programs-d1 config; at that point the configured target/secrets were not available locally, so deployed proof remained deferred and no authenticated E2E claim was made.
- Impeccable detector: `node /Users/noah.wong/.dotfiles/ai/agents/skills/impeccable/scripts/detect.mjs --json web/app/programs/page.tsx web/lib/programs/programs-boundary.tsx web/app/programs/programs.module.css` returned `[]` (no blocking findings).
- Bounded visual pass: mobile 390×844 screenshot captured at `/var/folders/pw/6vk89mjx4klgcgc8yp48y_lm0000gp/T/omp-sshots-15531d65f21f39ec.png`; desktop 1280×900 local shell/recovery screenshot captured at `/var/folders/pw/6vk89mjx4klgcgc8yp48y_lm0000gp/T/omp-sshots-15531d7a14742b67.webp`. Mobile inspection confirmed the Official Civic Minimal surface, Cantonese-first copy, 44px mode control, and phone bottom navigation. Local Next dev did not expose a live Worker API, so the visual pass observed the boundary loading/recovery states rather than claiming authenticated deployed proof.
- Deployment proof was previously deferred under the old policy. ADR-0029 now makes the local `wrangler dev` + D1 browser run the required implementation gate; any fresh deployed smoke remains optional operational evidence.
- Stable navigation is now a distinct server `/auth/me` `navigation` projection (`home`, `programs`, `events`, `scanner`, `profile`) consumed verbatim by `NavBar`; `sections[]` remains authorization (`Member`: `home`, `profile`, `programs`, with no Events operator access) and includes `home` for the authenticated placeholder route.
- `pnpm --dir web exec vitest run --config vitest.config.ts lib/sections.test.ts lib/api.test.ts` — 2 files, 37 tests passed; `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/app.test.tsx` — 1 file, 71 tests passed; `pnpm --dir web exec vitest run --config vitest.config.ts worker.auth.test.ts --pool=forks --maxWorkers=1` — 1 file, 42 tests passed. These assert identical Member/Admin stable navigation order, server-authorized section separation, and deterministic `firstSection` Profile recovery.
- `pnpm --dir web build` — passed and prerendered the authenticated `/home` route alongside existing sections.
- `pnpm test:shell-responsive` — 38 passed, 1 pre-existing viewport-inapplicable test skipped across the 3 configured viewports; stable `/home` active-nav behavior and the split `/auth/me` fixture passed.
- Historical remote-smoke note: `pnpm exec playwright test --config=tests/e2e/programs-d1.config.ts` was attempted against `https://efcc-dev-testing.efcc-ggc.workers.dev` with the fixed disposable credentials; all 10 tests failed because that deployment still served the legacy nested Programs manager (failure evidence: `test-results/programs-d1-PUI-01-deploye-860a9-ity-shaped-Management-entry-phone/error-context.md`). This remains historical operational evidence; the current local READY gate is recorded above and no deployed promotion claim is made.
- Historical visual/review note before the post-review polish: the local static pass used stubbed same-origin auth/Programs responses, and the earlier review used the then-current head. The current semantic and Worker/D1 evidence is recorded in `## Current post-review verification` above.


## Local-first policy follow-up

- User decision: the required `READY` gate is the relevant deterministic checks plus 100% local Playwright E2E against `wrangler dev` + local D1; Cloudflare deployment is optional/manual production-promotion evidence. The retired Apps Script `/exec` Playwright/storage-state suite is removed; deterministic `tests/gas/` coverage remains.
- User decision: local seed data is minimal and realistic — one `E2E_DEMO_` department, three `E2E_DEMO_` programs, and generated events for one recurring program. `pnpm db:seed:demo` is loopback-only and GET-first/idempotent.
- Accessibility root cause: the loading branch passed `COPY.programs.pageTitle` into `StatePanel`, producing a duplicate `課程與活動` `<h2>` beside the stable frame `<h1>`. The fix removes only that loading `title` prop; the frame heading, status region, tablist, tabpanel, capability projection, and management entry remain unchanged.
- Legacy boundary: deleted `tests/e2e/playwright.config.ts`, `auth.ts`, `role-matrix.test.ts`, `nested-task-navigation.test.ts`, `form-protection.test.ts`, and `tests/e2e/lib/deploy-acceptance*`; kept D1 Worker suites, responsive tests, `tests/gas/`, and the manual evidence appender.

## Executed results — local Programs D1

- Generated: 2026-08-11T08:55:43.355Z
- Target: http://127.0.0.1:8787/
- Total assertions: 10 | Passed: 10 | Failed: 0

| Role | Assertion | Result | Detail |
|------|-----------|--------|--------|
| phone | admin enters Participant mode with capability-shaped Management entry | PASS |  |
| phone | staff also enters Participant mode before any management action | PASS |  |
| phone | member enters Participant mode without a management gateway | PASS |  |
| phone | mode switching preserves a valid Program intent and exposes tabpanel semantics | PASS |  |
| phone | malformed direct intent stays recoverable inside Programs | PASS |  |
| desktop | admin enters Participant mode with capability-shaped Management entry | PASS |  |
| desktop | staff also enters Participant mode before any management action | PASS |  |
| desktop | member enters Participant mode without a management gateway | PASS |  |
| desktop | mode switching preserves a valid Program intent and exposes tabpanel semantics | PASS |  |
| desktop | malformed direct intent stays recoverable inside Programs | PASS |  |
