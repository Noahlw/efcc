# SCN-01 (Ticket #257) — Real-Camera Self Check-In Acceptance Trace

**Date:** 2026-08-14
**Worktree:** `feat/real-camera-checkin-257` (stack bottom layer, base `main` @ `91753e52`)
**Spec authority:** Issues #241, #244, #245, #251, #257; ADR-0018 (HTTP boundary/RFC 9457), ADR-0023 (single-lock mutation/audit), ADR-0026 (D1 domain schema), ADR-0027 (audit outcomes), ADR-0029 (local-first readiness gate). ADR-0015 (external-camera-origin scanner) is explicitly **superseded** for this vertical by Spec #244 decision 8.
**Scope:** Rebuild `/scanner` as a real in-app camera-first, Self-mode-only, authenticated destination per Spec #244. Explicitly deferred to later stack layers: the Self/Assisted mode switch and pinned Program+Event context control (#258), Guest Check-In UI/flow changes beyond whatever the Self/guest panel split requires (#259), and attendance correction/void/roster UI (#260 — the Worker handlers already exist and are out of this ticket's edit surface).

> Camera hardware itself cannot be exercised inside this environment's CI. Per Spec #241 testing decision 5, the camera/QR decoder sits behind a narrow adapter so deterministic tests cover valid/invalid/ambiguous/unsupported outcomes; a representative mobile-browser Playwright project (`phone-375x667`) is the required local-gate proxy for "real-device" per ADR-0029, and true physical-device smoke remains manual/optional operator evidence (repo AGENTS.md Headless-Gate: local `wrangler dev` run is the required `READY` gate; Cloudflare deployment and any physical-device pass are optional, explicitly-scoped operator evidence, never fabricated here).

## Acceptance criteria

| # | Criterion | Assertion method |
|---|---|---|
| 257-1 | Scanner is a stable authenticated top-level destination and always opens in Self mode for Member, Program Leader, Department Manager, Staff, and Admin | Component test with five role-shaped bootstrap fixtures asserting `/scanner` renders the Self surface directly (no gate/denial) and NavBar exposes the Scanner destination for every role |
| 257-2 | Browser requests/releases a real camera safely, decodes supported Program QR payloads through a narrow adapter, never presents static camera artwork as success | Unit tests for the extracted entry-decoding adapter (valid program token, valid manual code, malformed payload, unsupported/empty value); `useQrCamera` lifecycle tests (mount/permission-denied/unmount releases all tracks); component test asserting the success state only renders after a resolved server response, never from the raw camera frame |
| 257-3 | One eligible Event proceeds directly; several eligible Events open a concise date/time/location picker; no eligible Event explains recovery without adding navigation depth | Component tests with 0/1/N-event `resolveAttendance` fixtures; extend `tests/e2e/attendance-d1.test.ts` picker assertion to cover the (new) `location` field |
| 257-4 | Manual Event code fallback remains available when camera permission is denied, hardware is unsupported, decoding fails, or accessibility requires it | Component test simulating `onUnavailable`/permission failure and asserting the manual-code input remains reachable and receives focus |
| 257-5 | Enrollment, Event availability, cancellation, check-in window, duplicate attendance, session expiry, and current time are revalidated server-side before recording attendance | Worker-level D1 tests (extend `web/lib/attendance-worker.test.ts` pattern) for each rejection path against real `worker.fetch`; confirm existing self-check-in idempotency/enrollment/window coverage and add any missing `availability=Inactive` rejection case |
| 257-6 | Duplicate, invalid, denied, closed, offline, ambiguous transport, retry, and success states are accessible and preserve safe camera/task context | Component tests asserting `aria-live` status announcements and that mode/task context survives each outcome (pattern already established in `attendance-panel.test.tsx`) |
| 257-7 | Camera-adapter, state-machine, D1 duplicate, component, accessibility, representative mobile-browser, and deployed real-device smoke tests prove the journey | Adapter unit tests + Worker D1 tests + component/a11y tests + Playwright `phone-375x667` project (required local gate); physical real-device camera smoke recorded as explicitly deferred manual operator evidence, not fabricated |

## Test seams

- New narrow QR/manual-entry adapter extracted from `attendance-panel.tsx`'s `entryFromValue` (exact module path decided by implementer) — pure-function unit tests.
- `web/lib/use-qr-camera.ts` — existing hook, extend tests only if its contract changes.
- Rebuilt Self-mode surface (component split out of `attendance-panel.tsx`) + `web/app/scanner/page.tsx` — component tests via `vitest.components.config.ts` + MSW against the exact `/api/v1/attendance/resolve` and `/api/v1/attendance/self` routes.
- `web/lib/attendance.ts` `handleResolve`/`handleSelfCheckIn`/`openEvents`/`findEvent` — Worker-level tests via `attendance-worker.test.ts` pattern; covers the `events.name`/`events.location` column addition needed for the picker.
- `web/lib/sections.ts` / NavBar interaction for Scanner reachability across all five roles.
- `tests/e2e/attendance-d1.config.ts` suite — extend with Self-mode-only-entry and location-bearing-picker assertions; do not duplicate existing coverage (guest/assisted flows stay owned by later layers).

## Verification record

Append exact commands, pass counts, and any deferred evidence here after implementation, per ADR-0029 (local `wrangler dev` + local D1 is the required `READY` gate; Cloudflare deployment is optional operator evidence only).
- `pnpm typecheck` — PASS (`tsc --noEmit -p tsconfig.json` and `tsc --noEmit -p tests/e2e/tsconfig.json`).
- `pnpm --dir web typecheck` — PASS (`tsc --noEmit -p tsconfig.json` and `tsc --noEmit -p tsconfig.worker.json`).
- `pnpm --dir web test` — PASS: 18 files, 375 tests.
- `pnpm --dir web test:components` — PASS: 31 files, 339 tests.
- `pnpm --dir web exec vitest run lib/attendance-entry.test.ts lib/attendance-worker.test.ts` — PASS: 2 files, 25 tests.
- `pnpm dev:local` — READY at `http://localhost:8787` via the required local Worker/D1 gate; this environment required the Node 22 executable wrapper and `web/.dev.vars` local-only `EFCC_ACCESS_TOKEN_SECRET` because the supervised shell otherwise selected Node 20 and Wrangler closed auth with 503.
- `pnpm db:seed:local` — PASS: reset and seed SQL applied successfully (17 and 5 D1 statements).
- `pnpm exec playwright test -c tests/e2e/attendance-d1.config.ts` — PASS: 26/26 tests (13 phone-375x667 and 13 desktop-1280x720).
- Operate evidence: the live local Worker rendered the shared camera-first guest surface with no static camera artwork, and the Playwright phone/desktop projects exercised the authenticated `/scanner` Self route, picker/location, success, duplicate, denied, and recovery flows. Physical real-device camera permission/decoder smoke remains deferred manual operator evidence per ADR-0029; it is not fabricated here.
- Final rerun `pnpm --dir web test:components` — PASS: 31 files, 341 tests (an initial parallel-suite focus assertion was flaky; the immediate isolated test and full rerun both passed).
- Final rerun `pnpm --dir web exec vitest run lib/attendance-entry.test.ts lib/attendance-worker.test.ts` — PASS: 2 files, 25 tests.
- Final local gate rerun `pnpm db:seed:local` — PASS: reset and seed SQL applied successfully (17 and 5 D1 statements).
- Final local gate rerun `pnpm exec playwright test -c tests/e2e/attendance-d1.config.ts` — PASS: 26/26 tests (13 phone-375x667 and 13 desktop-1280x720).
- Final rerun also proves the spec follow-ups: valid empty Program-token resolution, QR no-event recovery mapping, focus transfer for picker/retry states, and ambiguous transport retry context.
- Final recovery-copy check `pnpm --dir web test:components` — PASS: 31 files, 341 tests after explicitly adding manual-code recovery guidance to the no-event state.
- Final recovery-copy check `pnpm --dir web test` — PASS: 18 files, 375 tests.
- Post-review label consolidation `pnpm --dir web exec vitest run --config vitest.components.config.ts lib/self-check-in-panel.test.tsx lib/attendance-panel.test.tsx lib/attendance-entry.test.ts` — PASS: 2 files, 18 tests.
- Post-review label consolidation `pnpm --dir web typecheck` — PASS (`tsc --noEmit -p tsconfig.json` and `tsc --noEmit -p tsconfig.worker.json`).
- Post-review full component gate `pnpm --dir web test:components` — PASS: 31 files, 341 tests.
- Post-review full unit gate `pnpm --dir web test` — PASS: 18 files, 375 tests.
- Standards P3 resolved: `use-attendance-flow.ts` now imports `attendanceEventLabel` from `attendance-display.ts`, eliminating the drifting duplicate formatter.
- Post-lint cleanup `fnm exec --using=v22.18.0 pnpm typecheck` — PASS: root and E2E TypeScript projects.
- Post-lint cleanup `fnm exec --using=v22.18.0 pnpm exec ultracite check tests/e2e/attendance-d1.test.ts web/app/guest-check-in/page.tsx web/app/scanner/page.tsx web/lib/app.test.tsx web/lib/attendance-display.ts web/lib/attendance-entry.test.ts web/lib/attendance-entry.ts web/lib/attendance-operator-panel.test.tsx web/lib/attendance-panel.test.tsx web/lib/attendance-panel.tsx web/lib/attendance-worker.test.ts web/lib/attendance.ts web/lib/copy.ts web/lib/self-check-in-panel.test.tsx web/lib/self-check-in-panel.tsx web/lib/use-attendance-flow.ts web/lib/use-qr-camera.test.tsx web/vitest.components.config.ts` — PASS: all 18 ticket code files format/lint clean.
- Post-lint cleanup `fnm exec --using=v22.18.0 pnpm --dir web exec vitest run --config vitest.components.config.ts lib/use-qr-camera.test.tsx lib/attendance-panel.test.tsx lib/self-check-in-panel.test.tsx lib/app.test.tsx lib/attendance-operator-panel.test.tsx` — PASS: 5 files, 97 tests.
- Post-lint cleanup `fnm exec --using=v22.18.0 pnpm --dir web exec vitest run lib/attendance-entry.test.ts lib/attendance-worker.test.ts` — PASS: 2 files, 25 tests.
- Post-lint cleanup `fnm exec --using=v22.18.0 sh .husky/pre-commit` — PASS: lint-staged Ultracite/Oxfmt and root typecheck.
