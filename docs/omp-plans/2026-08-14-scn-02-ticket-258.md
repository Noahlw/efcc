# SCN-02 (Ticket #258) — Assisted Scanner with Pinned Event Context Acceptance Trace

**Date:** 2026-08-14
**Worktree:** `feat/scanner-assisted-258` (stacked on `feat/real-camera-checkin-257` @ `418fe9c4`)
**Spec authority:** Issues #241, #244, #249, #257, #258; ADR-0018 (HTTP boundary/RFC 9457), ADR-0023 (single-lock mutation/audit), ADR-0026 (D1 domain schema), ADR-0027 (audit outcomes), ADR-0029 (local-first readiness gate). ADR-0015 (external-camera-origin scanner) is superseded and must not be reused.
**Scope:** Add capability-gated Assisted mode to the stable authenticated `/scanner` destination. Operators deliberately switch from Self, select/revalidate an eligible scoped Program/Event, keep that context pinned in the bottom control, and use the existing Worker/D1 assisted check-in/member-search path without creating Enrollment. Migrate the existing `/events` operator entry/link only as required to make Scanner Assisted the canonical path. #259 Guest Check-In polish/handoff and #260 correction/void/privacy/roster completion are deferred.

> The local `wrangler dev` + disposable D1 browser run is the required READY gate under ADR-0029. Physical camera smoke is manual evidence only; no device/deployment result may be fabricated.

## Acceptance criteria

| # | Criterion | Assertion method |
|---|---|---|
| 258-1 | Self/Assisted switch appears only when current effective access permits attendance operation for at least one eligible Event; ordinary Members never see it | Worker/component fixtures for Member, Admin/Staff, exact Program Leader, Department Manager, revoked/cross-scope grants; assert switch visibility from server-shaped capability/event data, never role-name branching; direct unauthorized Worker requests remain denied |
| 258-2 | Assisted mode pins selected Program and Event in a compact bottom context control and offers only open, eligible, authorized Events | Component/DOM tests assert accessible tablist/tab semantics, pinned bottom context, Program+Event labels, open/availability/status/window filtering, and no unauthorized/cancelled/inactive options; local phone Playwright asserts bottom placement and context persistence |
| 258-3 | Event-scoped entry preselects context only after live capability/Event-state revalidation; changing context clears unsafe transient scans | Worker contract tests for authorized, revoked, cross-scope, inactive, cancelled, closed-window, stale Event links; component tests assert stale deep-link recovery and selected/scan reset on context change |
| 258-4 | Operators scan approved Member QR or search eligible Members by permitted identifiers within pinned Event | Component tests cover real camera hook adapter, QR lookup, name/phone/exact member QR search, loading/empty/error; Worker/D1 tests exercise `GET /members` and assisted POST through `worker.fetch`; no raw member data outside scope |
| 258-5 | Unenrolled, duplicate, inactive, cancelled, closed-window, forbidden, stale, and ambiguous outcomes are deterministic and never create Enrollment automatically | Worker/D1 outcome tests assert status/problem code, attendance row count, Enrollment row count unchanged, and audit outcome; component tests preserve queue/context and expose retry/recovery |
| 258-6 | Leaving Scanner, changing mode/context, logout, or unmount performs idempotent camera/listener cleanup; no stale scan submits | Hook/component tests assert all stream tracks stop and stale callbacks cannot submit after mode/context changes, logout, unmount, and repeated cleanup |
| 258-7 | Capability, cross-scope denial, camera, search, cleanup, audit, accessibility, and deployed queue-flow tests prove Assisted mode | Worker/D1 + component/a11y + local phone/desktop attendance Playwright coverage; optional deployed smoke only if an operator explicitly runs a fresh reserved target; no production credentials/data |

## Test seams

- `web/app/scanner/page.tsx` and the #257 `SelfCheckInPanel`/`useAttendanceFlow` contract — extend with explicit Self/Assisted mode state; Self remains the default for every authenticated account.
- `web/lib/programs/programs-boundary.tsx` / `programs-intent.ts` — mirror existing accessible tablist, URL intent, focus restoration, and capability projection patterns; do not branch on account role names.
- `web/lib/attendance-operator-panel.tsx` — reuse member search, assisted submit, roster/context sub-surfaces only after moving entry into Scanner Assisted; eliminate raw event-id input as the canonical operator entry.
- `web/lib/attendance.ts` / `web/worker.ts` — keep the historical `/api/v1/attendance/events` chooser for `/events`, add the open-only `/api/v1/attendance/scanner-events` projection for Scanner, and reuse `handleSearchMembers`/`handleAssistedCheckIn`; Server authorization remains authoritative.
- `web/lib/programs/program-api.ts` — cookie-only same-origin fetch wrappers and Attendance DTOs; preserve RFC 9457 error mapping.
- `web/lib/sections.ts`, `web/lib/nav-bar.tsx`, `web/app/events/page.tsx`, `web/lib/programs/programs-events-panel.tsx` — migrate operator links/route consumers coherently; do not remove a stable navigation destination unless the current route is proven to be only the superseded operator surface.
- `web/lib/use-qr-camera.ts` — real camera lifecycle; no static camera success or external-origin scanner.
- `tests/e2e/attendance-d1.test.ts` — extend the existing local Worker/D1 suite rather than adding a second config; assert Admin/operator Assisted switch, Member absence, pinned context, deep-link revalidation, search/check-in, no Enrollment, and cleanup-relevant observable states.

## Verification record

Append exact commands, pass counts, review findings/resolutions, stack/PR evidence, and any deferred physical-device/deployed evidence after implementation. The local `wrangler dev` + D1 Playwright run is the required READY gate.

## Verification results

- Worker/D1 contract: `fnm exec --using=v22.18.0 pnpm --dir web exec vitest run --config vitest.config.ts lib/attendance-worker.test.ts` — **31 passed**. Covers capability projection, Member denial, scoped grants, revoked grant exclusion, cross-scope denial, inactive/cancelled/closed Events across resolve/self/guest/assisted-search/assisted-POST paths, inclusive UTC window boundary, deterministic duplicate/unenrolled outcomes, audit responses, and unchanged Enrollment rows.
- Scanner/component contract: `fnm exec --using=v22.18.0 pnpm --dir web exec vitest run --config vitest.components.config.ts lib/assisted-scanner-panel.test.tsx lib/scanner-boundary.test.tsx lib/scanner-intent.test.ts lib/use-qr-camera.test.tsx lib/attendance-operator-panel.test.tsx lib/self-check-in-panel.test.tsx` — **6 files, 37 passed** (including Assisted search loading/empty/error states). Programs Events panel compatibility suite — **11 passed**.
- Type checks: `fnm exec --using=v22.18.0 pnpm typecheck` — passed; `fnm exec --using=v22.18.0 pnpm --dir web typecheck` — passed.
- Ticket lint subset: `fnm exec --using=v22.18.0 pnpm exec ultracite check` over all changed/new #258 files except the two unchanged-baseline portions of `web/lib/programs/program-api.ts` and `web/lib/programs/programs-events-panel.tsx` — **15 files passed**. Running the check on those two files also reports their pre-existing `no-dynamic-delete`, complexity, and `no-negated-condition` findings; the #258 diff in them is comment/link-only and does not introduce those findings. `git diff --check` — passed.
- Local required queue flow: supervised `wrangler dev` at `127.0.0.1:8787`, disposable D1 reseeded, then `fnm exec --using=v22.18.0 pnpm exec playwright test -c tests/e2e/attendance-d1.config.ts` — **30 passed (15 phone, 15 desktop)**. This includes Admin Assisted switch/pinned context/search/check-in/no-Enrollment, Member Self-only visibility, and cancelled Event deep-link revalidation.
- Compatibility decision: `/events` remains available for its stable Events browser plus roster/void/correction surfaces; the Programs Events assisted action now emits `/scanner?mode=assisted&event=...`. Scanner owns the canonical Assisted entry, and raw event-id entry remains only on the preserved legacy surface.
- Eligibility/window decision: the legacy `GET /api/v1/attendance/events` chooser remains historical and scoped; `GET /api/v1/attendance/scanner-events` projects only server-authorized Active Program/Event/availability rows whose live check-in window is open, with inclusive UTC boundaries. Assisted member search and POST revalidate the active Program/Event/window gate, and Assisted POST is window-gated (no legacy bypass).
- Review resolutions: camera generations stop late `getUserMedia` streams and survive StrictMode replay; Assisted camera restart is disabled while open; successful search moves focus to an `aria-live` result list; access-probe errors announce and focus retry; changing mode/context and unmount cleanup are idempotent; no Assisted path creates Enrollment.
- Deferred evidence: no physical camera/device run and no deployed-target smoke was performed. Local browser coverage uses the real camera hook seam with permission-free fallback; Safari manual inspection was blocked by its local HTTP Secure-cookie session behavior after the seeded login returned `200` but `/api/v1/auth/me` remained `401`. No production credentials or data were used.
