# EVT-02 — Recurring preview and generation acceptance trace

## Scope

- Ticket: #252
- Base: `origin/main` at `b7f1842741d61b7f15bf4b8809a3e69a2fe9e1d0`
- Public surface: the reachable Programs management workspace. Do not extend the unreferenced legacy `programs-manager` / `programs-events-panel` path.
- Local gate: `wrangler dev` at `127.0.0.1:8787` with disposable `E2E_`/`E2E_DEMO_` D1 fixtures only.

## Observable setup

1. Start the local app with `pnpm dev:local`.
2. Seed the disposable fixtures with the checked-in local seed command.
3. Log in as a scoped Manager or Program Leader with Program Manage capability.
4. Open Management → the target program → its reachable Events/Settings workspace.

## Acceptance criteria

### EVT-02.1 — Preview is exact and non-mutating

- Create or edit weekly and monthly recurring rules with explicit Hong Kong wall-clock time, location, horizon, and boundary dates.
- Add a cancel exception and a reschedule exception.
- Choose **Preview** from the reachable workspace.
- The response/DOM shows a server-owned plan identity and exact occurrence rows: local date, local time, location, source rule, and exception/skip state.
- Before and after preview, the observable event directory contains the same rows and the audit stream contains no generation success or created-event records.
- Zero schedule rules produces an explicit failed response and failed audit record; it does not silently return an empty successful plan.

### EVT-02.2 — Plan identity prevents stale writes

- Preview the current rules and record the plan identity/hash.
- Change a rule or exception, then attempt to generate with the old plan.
- The response is a deterministic stale-plan error; no event is written and the UI refreshes or requires a new preview.
- Repeating Preview without changing inputs returns the same occurrence ordering and plan identity.

### EVT-02.3 — Generation is authorized, idempotent, and auditable

- Generate from a current valid preview as the authorized Manager/Program Leader.
- The event directory shows only occurrences authorized by the current plan, with shared event detail links.
- Repeating the same generation is deterministic: no duplicate event rows, explicit created/skipped counts, and an audit/generation-run record.
- A user without Program Manage or outside the program scope receives a forbidden result and creates neither events nor generation records.

### EVT-02.4 — Recovery and concurrency are safe

- Exercise retry after a generation run has recorded a partial/failed unit. Retry resumes from durable run state and never duplicates already-created events.
- Start two equivalent generation attempts concurrently. At most one event exists for each unique program/start pair; both responses expose deterministic created/skipped or conflict outcomes.
- A malformed/ambiguous preview is rejected before event writes and leaves the event directory unchanged.

### EVT-02.5 — Reachability and accessibility

- All controls above are reachable from the current Program Workspace in the browser without navigating through the legacy manager.
- Loading, empty, error, stale, forbidden, and success states are visible in the DOM with accessible names and keyboard operation.
- The shared event directory and event detail remain usable at narrow viewport widths.

## Required evidence

- Focused worker/store/component tests for the criteria above.
- Local Playwright trace against `wrangler dev` + local D1, with each criterion asserted through DOM or response state.
- Evidence paths recorded in this file after verification; no Cloudflare account or Apps Script/Google Sheet mutation.

## Implementation evidence (committed with #252)

Commit: `HEAD` of `feat/recurring-event-generation-252` (see `git log`), based on
`origin/main` `b7f1842741d61b7f15bf4b8809a3e69a2fe9e1d0` + the trace commit.

### Focused verification commands (all passed locally)

| Command | Result |
| --- | --- |
| `pnpm --dir web typecheck` | pass (Next app + worker tsconfigs) |
| `pnpm --dir web exec vitest run lib/programs/programs.test.ts` | 120/120 passed |
| `pnpm --dir web exec vitest run --config vitest.components.config.ts` | 305/305 passed (27 files) |
| `pnpm --dir web exec vitest run worker.test.ts worker.auth.test.ts` | 75/75 passed |
| `pnpm typecheck` (root, includes `tests/e2e/tsconfig.json`) | pass |

### Criterion → evidence mapping

- **EVT-02.1 Preview is exact and non-mutating** — worker: `EVT-02.1 preview materializes exact weekly/monthly occurrences with locations and exceptions, without writing events` (asserts weekly + monthly rows, HK wall times, rule locations on every row, CANCEL skip rows, RESCHEDULE overrides, deterministic `plan_id:rule_id:date` occurrence identity, ascending ordering, repeat preview returns same plan identity + ordering, zero events written, no `EVENT_GENERATE`/`EVENT_CREATE` audits, `EVENT_PREVIEW` SUCCESS audit) and `EVT-02.1 rule locations flow into generated events`; zero rules → `preview on a program with no schedule rules returns 422 with a FAILED audit row`. Component DOM: `EVT-02 recurring preview and generation UI` → plan rows, location, 本次已取消 badge, plan identity, generate control all reachable in the events task.
- **EVT-02.2 Plan identity prevents stale writes** — worker: `EVT-02.2 generation with a stale plan is rejected before writes` (rule PATCH → old plan rejected 409 `STALE_PLAN`, event count unchanged, `EVENT_GENERATE` FAILED audit, changed rules produce a new plan identity, fresh plan generates 200).
- **EVT-02.3 Generation authorized, idempotent, auditable** — worker: `EVT-02.3 generation is idempotent, deterministic, and audited` (repeat reuses the same `run_id`, created 0/skipped 2, unique `(program, starts_at)`, durable `program_generation_runs` row, `EVENT_GENERATE` SUCCESS audit), `EVT-02.3 a member without Program Manage is forbidden and writes nothing` (403 `FORBIDDEN`, zero events, zero run rows), `EVT-02.3 CANCEL exceptions are skipped and RESCHEDULE moves the generated event` (skipped=1 with durable `CANCEL` detail, no event on the CANCEL date, RESCHEDULE wall time applied).
- **EVT-02.4 Recovery and concurrency are safe** — worker: `EVT-02.4 generation resumes from a durable partial run without duplicating events` (pre-seeded partial run with one `failed` item → retry resumes same run, retries the failed unit, completed, no duplicates), `EVT-02.4 two concurrent generation attempts create at most one event per start` (Promise.all → one run, unique starts, counts sum to occurrences), `EVT-02.4 malformed or unknown plans are rejected before writes` (404 `PLAN_NOT_FOUND`, 422 missing `plan_id`, zero events/runs).
- **EVT-02.5 Reachability and accessibility** — component DOM tests (jsdom): preview controls reachable only for Recurring + manage, loading/empty/error/stale states asserted through accessible names and `role="alert"` output, generate disabled until a current plan exists, stale-plan error clears the plan and requires a new preview; full component suite 305/305 keeps the shared event directory/detail links green.

### Review-fix follow-up (applied on top of the first implementation commit)

Commit: `HEAD` of `feat/recurring-event-generation-252` (second commit, see `git log`).

Addresses the two-axis review findings:

1. Migration renamed to `0008_event_preview_generation.sql` (base `b7f18427` already ships
   `0007_department_managers.sql`); local application confirmed by the workerd vitest pool
   (`applyMigrations` applies 0000–0008 in order — 124/124 worker tests pass on the migrated schema).
2. All preview/run FKs now `ON DELETE RESTRICT` (no CASCADE/SET NULL): deleting an exception,
   rule, or plan can never silently erase durable preview/run evidence. `exception_id` on preview
   occurrences is snapshot attribution without an FK, so the preserved trunk “restore exception”
   flow keeps working while stale plans remain and are rejected at generation (409 STALE_PLAN).
3. Preview exception selection is rule-scoped (`rule_id + override_date` keyed); same-date
   exceptions on one rule never cross-affect another rule — regression test added.
4. Repeated completed generation emits its own `EVENT_GENERATE` SUCCESS audit with
   created=0/skipped>0 (ADR-0027); stale/empty-plan business conflicts audit `CONFLICT`, not
   FAILED; zero-rule preview keeps FAILED.
5. Generation attribution: after an ignored `INSERT OR IGNORE`, the attempt verifies the event
   row actually exists — a genuine duplicate records `skipped` WITH the existing event_id; any
   other swallowed constraint records `failed`, never a false skip. `event_id` on run items is a
   plain index (the same event is legitimately referenced by its creating run and later skipped
   runs). `finishGenerationRun` now settles counts/status atomically from the item table with a
   `finished_at IS NULL` compare-and-set; every caller reloads the settled row so the run and all
   responses converge.
6. Rule `location` clearing persists (explicit null is distinguished from absent via a sentinel
   CASE), and malformed non-string location fails closed with 422 — regression tests added.
7. Preview DTO projects `created_by` out (`PreviewPlanView`); `EmptyPreviewPlanError` is
   generic (no plan UUID leak).

Focused checks (post-fix): `pnpm --dir web typecheck` pass; worker programs suite 124/124
(+4 regression tests); component suite 305/305; worker/auth suites 75/75; root typecheck
(incl. `tests/e2e`) pass.

### Local Playwright evidence (run on this branch, port 8788)

Stack: `pnpm --dir web build` + `pnpm --dir web db:migrate:local` (0000–0008 applied in
order) + `wrangler dev --port 8788` + `pnpm db:seed:local` + `DEMO_TARGET_URL=http://127.0.0.1:8788 pnpm db:seed:demo`
(seed-demo exercised the migrated preview→generate flow: "Generated events for
E2E_DEMO_成人查經: 12").

| Command | Result |
| --- | --- |
| `PROGRAMS_TARGET_URL=http://127.0.0.1:8788 pnpm exec playwright test -c tests/e2e/programs-d1.config.ts` | 72/72 passed (phone + desktop), incl. 4 new EVT-02 tests |
| `PROGRAMS_TARGET_URL=http://127.0.0.1:8788 pnpm exec playwright test -c tests/e2e/attendance-d1.config.ts` | 24/24 passed (both projects) after adding the required `category: "測試"` to the fixture's program payload (the pre-existing 422 omission) — the fixture's #252 preview→generate migration now runs end to end |

New EVT-02 Playwright coverage (`tests/e2e/programs-d1.test.ts`): preview materializes
plan rows without writing events (DOM rows + unchanged event count), generation reports
deterministic created/skipped counts and refreshes the directory (fresh disposable
program; repeat shows the resumed notice with created=0), stale plan is rejected with
`時間表已變更，請重新預覽。` and clears the generate control, and a member without Program
Manage sees the no-management-scope boundary with zero preview/generate controls.
Pre-existing heading locators were tightened to `exact: true` because the preview panel's
heading legitimately contains 「聚會」.

Artifacts: `tests/e2e/test-results/programs-d1-results.json` (+ per-test trace/screenshot
archives on failure). READY browser evidence remains coordinator-owned at
`http://127.0.0.1:8787` per the gate below.

### Seed reset regression fix (committed follow-up)

`pnpm db:seed:local` (checked-in `tests/e2e/seed-dev-accounts.ts --reset`) previously
failed with `FOREIGN KEY constraint failed` once EVT-02 preview/run rows existed, because
the reset deleted programs/rules/events (parents) before the new RESTRICT children. Fixed
by deleting the EVT-02 tables first, children before parents:

1. `program_generation_run_items` (scoped via runs of `E2E_*` programs)
2. `program_generation_runs`
3. `program_preview_occurrences`
4. `program_preview_plans`

then the existing exceptions/rules/attendances/events/enrollments/requests/leaders/
programs/modules/managers/departments/registration-requests deletes. RESTRICT is
unchanged. Verified against a local D1 that held real EVT-02 plans/runs/items from the
Playwright run: `pnpm db:seed:local` now succeeds and leaves plans/runs/items/
occurrences/programs/events all at 0. Added worker regression `EVT-02.6 checked-in seed
reset order clears preview/run rows before their parents` (replays the reset statements
verbatim against the schema and asserts the E2E_ program and all durable rows are gone).

### Coordinator READY verification (local gate, per `tests/e2e/README.md`)

```sh
# terminal 1
pnpm dev:local                                   # build + migrate + wrangler dev on 127.0.0.1:8787
# terminal 2
pnpm db:seed:local                               # E2E_ disposable accounts
pnpm db:seed:demo                                # E2E_DEMO_ department/programs/events (now preview→generate)
pnpm exec playwright test -c tests/e2e/programs-d1.config.ts
pnpm exec playwright test -c tests/e2e/attendance-d1.config.ts
```

Browser evidence paths (after a gate run): `tests/e2e/test-results/programs-d1-results.json`,
`tests/e2e/test-results/attendance-d1-results.json`, plus the Playwright trace archives in
`tests/e2e/test-results/`. No Cloudflare account or Apps Script/Google Sheet mutation: the seed
scripts refuse non-loopback targets and the gate runs entirely against local D1 `E2E_`/`E2E_DEMO_`
fixtures.

## Coordinator READY Programs results

- Generated: 2026-08-13T07:03:56.089Z
- Target: http://127.0.0.1:8787/
- Total assertions: 72 | Passed: 72 | Failed: 0

| Role | Assertion | Result | Detail |
|------|-----------|--------|--------|
| phone | admin enters Participant mode with capability-shaped Management entry | PASS |  |
| phone | staff also enters Participant mode before any management action | PASS |  |
| phone | member enters Participant mode without a management gateway | PASS |  |
| phone | mode switching preserves a valid Program intent and exposes tabpanel semantics | PASS |  |
| phone | malformed direct intent stays recoverable inside Programs | PASS |  |
| phone | restores a direct Programs intent after session expiry and login | PASS |  |
| desktop | admin enters Participant mode with capability-shaped Management entry | PASS |  |
| desktop | staff also enters Participant mode before any management action | PASS |  |
| desktop | member enters Participant mode without a management gateway | PASS |  |
| desktop | mode switching preserves a valid Program intent and exposes tabpanel semantics | PASS |  |
| desktop | malformed direct intent stays recoverable inside Programs | PASS |  |
| desktop | restores a direct Programs intent after session expiry and login | PASS |  |
| phone | member sees Listed catalog rows and never the Unlisted fixture | PASS |  |
| phone | admin sees the Unlisted fixture through scoped management access | PASS |  |
| phone | search narrows the catalog and clearing restores the same rows | PASS |  |
| phone | empty search result is recoverable by clearing | PASS |  |
| phone | row selection hands off through the canonical Program intent URL | PASS |  |
| desktop | member sees Listed catalog rows and never the Unlisted fixture | PASS |  |
| desktop | admin sees the Unlisted fixture through scoped management access | PASS |  |
| desktop | search narrows the catalog and clearing restores the same rows | PASS |  |
| desktop | empty search result is recoverable by clearing | PASS |  |
| desktop | row selection hands off through the canonical Program intent URL | PASS |  |
| phone | direct detail survives refresh and returns to the directory | PASS |  |
| phone | member receives privacy-preserving unavailable state for Unlisted detail | PASS |  |
| desktop | direct detail survives refresh and returns to the directory | PASS |  |
| desktop | member receives privacy-preserving unavailable state for Unlisted detail | PASS |  |
| phone | member can submit one request, see Pending, and withdraw it | PASS |  |
| phone | ManagerOnly detail explains that participants cannot self-enroll | PASS |  |
| desktop | member can submit one request, see Pending, and withdraw it | PASS |  |
| desktop | ManagerOnly detail explains that participants cannot self-enroll | PASS |  |
| phone | admin searches the scoped Directory and navigates focused Workspace tasks | PASS |  |
| phone | keeps Directory and Workspace entry points keyboard-operable | PASS |  |
| phone | member direct management links stay out of scope | PASS |  |
| phone | staff uses the same capability-shaped Directory information architecture | PASS |  |
| phone | leader exact scope and manager inheritance stay distinct | PASS |  |
| phone | a revoked or unknown direct management link stays generic | PASS |  |
| desktop | admin searches the scoped Directory and navigates focused Workspace tasks | PASS |  |
| desktop | keeps Directory and Workspace entry points keyboard-operable | PASS |  |
| desktop | member direct management links stay out of scope | PASS |  |
| desktop | staff uses the same capability-shaped Directory information architecture | PASS |  |
| desktop | leader exact scope and manager inheritance stay distinct | PASS |  |
| desktop | a revoked or unknown direct management link stays generic | PASS |  |
| phone | renders all scope-owned groups and omits recurring controls for OneOff | PASS |  |
| phone | renders unavailable copy for Schedule and Attendance when their modules are disabled | PASS |  |
| phone | consequential discoverability change requires confirmation before it saves | PASS |  |
| desktop | renders all scope-owned groups and omits recurring controls for OneOff | PASS |  |
| desktop | renders unavailable copy for Schedule and Attendance when their modules are disabled | PASS |  |
| desktop | consequential discoverability change requires confirmation before it saves | PASS |  |
| phone | Staff denies self-assignment, revokes the seeded leader, and re-grants it | PASS |  |
| desktop | Staff denies self-assignment, revokes the seeded leader, and re-grants it | PASS |  |
| phone | Admin grants a Department Manager, scope inherits, then revokes | PASS |  |
| desktop | Admin grants a Department Manager, scope inherits, then revokes | PASS |  |
| phone | creates a OneOff, operates multiple Events, edits, and blocks archive | PASS |  |
| phone | member direct Program mutation is denied server-side | PASS |  |
| desktop | creates a OneOff, operates multiple Events, edits, and blocks archive | PASS |  |
| desktop | member direct Program mutation is denied server-side | PASS |  |
| phone | admin creates, deep-links, and edits an event with HK wall display | PASS |  |
| phone | safe deactivation is immediate with Undo; cancellation retires controls | PASS |  |
| phone | an active Program enrollment alone does not gate this event's deactivation | PASS |  |
| phone | a currently open check-in window with zero check-ins still requires confirmation to deactivate | PASS |  |
| desktop | admin creates, deep-links, and edits an event with HK wall display | PASS |  |
| desktop | safe deactivation is immediate with Undo; cancellation retires controls | PASS |  |
| desktop | an active Program enrollment alone does not gate this event's deactivation | PASS |  |
| desktop | a currently open check-in window with zero check-ins still requires confirmation to deactivate | PASS |  |
| phone | preview materializes exact rows without writing events | PASS |  |
| phone | generation reports deterministic created/skipped counts and refreshes the directory | PASS |  |
| phone | a stale plan is rejected before writes and requires a fresh preview | PASS |  |
| phone | preview/generate controls are unreachable without the manage capability | PASS |  |
| desktop | preview materializes exact rows without writing events | PASS |  |
| desktop | generation reports deterministic created/skipped counts and refreshes the directory | PASS |  |
| desktop | a stale plan is rejected before writes and requires a fresh preview | PASS |  |
| desktop | preview/generate controls are unreachable without the manage capability | PASS |  |
## Coordinator READY Attendance results

- Generated: 2026-08-13T07:04:01.248Z
- Target: http://127.0.0.1:8787/
- Total assertions: 24 | Passed: 24 | Failed: 0

| Role | Assertion | Result | Detail |
|------|-----------|--------|--------|
| phone-375x667 | A guest check-in happy path: entry → chooser → name/phone → success | PASS |  |
| phone-375x667 | B guest same phone 200 {outcome duplicate} + neutral notice | PASS |  |
| phone-375x667 | C guest void (with reason) then re-check-in succeeds (201) | PASS |  |
| phone-375x667 | D member self check-in on /scanner: success then quiet duplicate | PASS |  |
| phone-375x667 | E assisted check-in: member search (name + seeded QR) → 替成員簽到 → roster | PASS |  |
| phone-375x667 | F cancelled event: resolve surfaces 410 EVENT_CANCELLED in UI and API | PASS |  |
| phone-375x667 | G unknown entry: UI 404 surface, check-in POST 403 INVALID_CHECK_IN_ENTRY | PASS |  |
| phone-375x667 | H member without enrollment: 403 ENROLLMENT_REQUIRED in UI and API | PASS |  |
| phone-375x667 | I check-in sheet export controls render for the operator panel | PASS |  |
| phone-375x667 | J cancelled event on the operator panel: notice, readable roster, and no check-in controls | PASS |  |
| phone-375x667 | K empty guest submit: native validation blocks the request | PASS |  |
| phone-375x667 | L invalid phone surfaces the server VALIDATION detail with the error tone | PASS |  |
| desktop-1280x720 | A guest check-in happy path: entry → chooser → name/phone → success | PASS |  |
| desktop-1280x720 | B guest same phone 200 {outcome duplicate} + neutral notice | PASS |  |
| desktop-1280x720 | C guest void (with reason) then re-check-in succeeds (201) | PASS |  |
| desktop-1280x720 | D member self check-in on /scanner: success then quiet duplicate | PASS |  |
| desktop-1280x720 | E assisted check-in: member search (name + seeded QR) → 替成員簽到 → roster | PASS |  |
| desktop-1280x720 | F cancelled event: resolve surfaces 410 EVENT_CANCELLED in UI and API | PASS |  |
| desktop-1280x720 | G unknown entry: UI 404 surface, check-in POST 403 INVALID_CHECK_IN_ENTRY | PASS |  |
| desktop-1280x720 | H member without enrollment: 403 ENROLLMENT_REQUIRED in UI and API | PASS |  |
| desktop-1280x720 | I check-in sheet export controls render for the operator panel | PASS |  |
| desktop-1280x720 | J cancelled event on the operator panel: notice, readable roster, and no check-in controls | PASS |  |
| desktop-1280x720 | K empty guest submit: native validation blocks the request | PASS |  |
| desktop-1280x720 | L invalid phone surfaces the server VALIDATION detail with the error tone | PASS |  |


## Coordinator READY Programs results (post-fix)

- Generated: 2026-08-13T08:45:16.450Z
- Target: http://127.0.0.1:8791/
- Total assertions: 72 | Passed: 72 | Failed: 0

| Role | Assertion | Result | Detail |
|------|-----------|--------|--------|
| phone | admin enters Participant mode with capability-shaped Management entry | PASS |  |
| phone | staff also enters Participant mode before any management action | PASS |  |
| phone | member enters Participant mode without a management gateway | PASS |  |
| phone | mode switching preserves a valid Program intent and exposes tabpanel semantics | PASS |  |
| phone | malformed direct intent stays recoverable inside Programs | PASS |  |
| phone | restores a direct Programs intent after session expiry and login | PASS |  |
| desktop | admin enters Participant mode with capability-shaped Management entry | PASS |  |
| desktop | staff also enters Participant mode before any management action | PASS |  |
| desktop | member enters Participant mode without a management gateway | PASS |  |
| desktop | mode switching preserves a valid Program intent and exposes tabpanel semantics | PASS |  |
| desktop | malformed direct intent stays recoverable inside Programs | PASS |  |
| desktop | restores a direct Programs intent after session expiry and login | PASS |  |
| phone | member sees Listed catalog rows and never the Unlisted fixture | PASS |  |
| phone | admin sees the Unlisted fixture through scoped management access | PASS |  |
| phone | search narrows the catalog and clearing restores the same rows | PASS |  |
| phone | empty search result is recoverable by clearing | PASS |  |
| phone | row selection hands off through the canonical Program intent URL | PASS |  |
| desktop | member sees Listed catalog rows and never the Unlisted fixture | PASS |  |
| desktop | admin sees the Unlisted fixture through scoped management access | PASS |  |
| desktop | search narrows the catalog and clearing restores the same rows | PASS |  |
| desktop | empty search result is recoverable by clearing | PASS |  |
| desktop | row selection hands off through the canonical Program intent URL | PASS |  |
| phone | direct detail survives refresh and returns to the directory | PASS |  |
| phone | member receives privacy-preserving unavailable state for Unlisted detail | PASS |  |
| desktop | direct detail survives refresh and returns to the directory | PASS |  |
| desktop | member receives privacy-preserving unavailable state for Unlisted detail | PASS |  |
| phone | member can submit one request, see Pending, and withdraw it | PASS |  |
| phone | ManagerOnly detail explains that participants cannot self-enroll | PASS |  |
| desktop | member can submit one request, see Pending, and withdraw it | PASS |  |
| desktop | ManagerOnly detail explains that participants cannot self-enroll | PASS |  |
| phone | admin searches the scoped Directory and navigates focused Workspace tasks | PASS |  |
| phone | keeps Directory and Workspace entry points keyboard-operable | PASS |  |
| phone | member direct management links stay out of scope | PASS |  |
| phone | staff uses the same capability-shaped Directory information architecture | PASS |  |
| phone | leader exact scope and manager inheritance stay distinct | PASS |  |
| phone | a revoked or unknown direct management link stays generic | PASS |  |
| desktop | admin searches the scoped Directory and navigates focused Workspace tasks | PASS |  |
| desktop | keeps Directory and Workspace entry points keyboard-operable | PASS |  |
| desktop | member direct management links stay out of scope | PASS |  |
| desktop | staff uses the same capability-shaped Directory information architecture | PASS |  |
| desktop | leader exact scope and manager inheritance stay distinct | PASS |  |
| desktop | a revoked or unknown direct management link stays generic | PASS |  |
| phone | renders all scope-owned groups and omits recurring controls for OneOff | PASS |  |
| phone | renders unavailable copy for Schedule and Attendance when their modules are disabled | PASS |  |
| phone | consequential discoverability change requires confirmation before it saves | PASS |  |
| desktop | renders all scope-owned groups and omits recurring controls for OneOff | PASS |  |
| desktop | renders unavailable copy for Schedule and Attendance when their modules are disabled | PASS |  |
| desktop | consequential discoverability change requires confirmation before it saves | PASS |  |
| phone | Staff denies self-assignment, revokes the seeded leader, and re-grants it | PASS |  |
| desktop | Staff denies self-assignment, revokes the seeded leader, and re-grants it | PASS |  |
| phone | Admin grants a Department Manager, scope inherits, then revokes | PASS |  |
| desktop | Admin grants a Department Manager, scope inherits, then revokes | PASS |  |
| phone | creates a OneOff, operates multiple Events, edits, and blocks archive | PASS |  |
| phone | member direct Program mutation is denied server-side | PASS |  |
| desktop | creates a OneOff, operates multiple Events, edits, and blocks archive | PASS |  |
| desktop | member direct Program mutation is denied server-side | PASS |  |
| phone | admin creates, deep-links, and edits an event with HK wall display | PASS |  |
| phone | safe deactivation is immediate with Undo; cancellation retires controls | PASS |  |
| phone | an active Program enrollment alone does not gate this event's deactivation | PASS |  |
| phone | a currently open check-in window with zero check-ins still requires confirmation to deactivate | PASS |  |
| desktop | admin creates, deep-links, and edits an event with HK wall display | PASS |  |
| desktop | safe deactivation is immediate with Undo; cancellation retires controls | PASS |  |
| desktop | an active Program enrollment alone does not gate this event's deactivation | PASS |  |
| desktop | a currently open check-in window with zero check-ins still requires confirmation to deactivate | PASS |  |
| phone | preview materializes exact rows without writing events | PASS |  |
| phone | generation reports deterministic created/skipped counts and refreshes the directory | PASS |  |
| phone | a stale plan is rejected before writes and requires a fresh preview | PASS |  |
| phone | preview/generate controls are unreachable without the manage capability | PASS |  |
| desktop | preview materializes exact rows without writing events | PASS |  |
| desktop | generation reports deterministic created/skipped counts and refreshes the directory | PASS |  |
| desktop | a stale plan is rejected before writes and requires a fresh preview | PASS |  |
| desktop | preview/generate controls are unreachable without the manage capability | PASS |  |
