# EVT-02 — Recurring preview and generation acceptance trace

## Scope

- Ticket: #252
- Base: `origin/main` at `b7f1842741d61b7f15bf4b8809a3e69a2fe9e1d0`
- Public surface: the reachable Programs management workspace. Do not extend the unreferenced legacy `programs-manager` / `programs-events-panel` path.
- Local gate: `wrangler dev` at `127.0.0.1:8787` with disposable `E2E_`/`E2E_DEMO_` D1 fixtures only.

## Observable setup

1. Start the local app with `pnpm dev:local`.
2. Seed the disposable fixtures with the checked-in local seed command.
3. Log in as a scoped Manager or Program Leader with Program Manage capability.
4. Open Management → the target program → its reachable Events/Settings workspace.

## Acceptance criteria

### EVT-02.1 — Preview is exact and non-mutating

- Create or edit weekly and monthly recurring rules with explicit Hong Kong wall-clock time, location, horizon, and boundary dates.
- Add a cancel exception and a reschedule exception.
- Choose **Preview** from the reachable workspace.
- The response/DOM shows a server-owned plan identity and exact occurrence rows: local date, local time, location, source rule, and exception/skip state.
- Before and after preview, the observable event directory contains the same rows and the audit stream contains no generation success or created-event records.
- Zero schedule rules produces an explicit failed response and failed audit record; it does not silently return an empty successful plan.

### EVT-02.2 — Plan identity prevents stale writes

- Preview the current rules and record the plan identity/hash.
- Change a rule or exception, then attempt to generate with the old plan.
- The response is a deterministic stale-plan error; no event is written and the UI refreshes or requires a new preview.
- Repeating Preview without changing inputs returns the same occurrence ordering and plan identity.

### EVT-02.3 — Generation is authorized, idempotent, and auditable

- Generate from a current valid preview as the authorized Manager/Program Leader.
- The event directory shows only occurrences authorized by the current plan, with shared event detail links.
- Repeating the same generation is deterministic: no duplicate event rows, explicit created/skipped counts, and an audit/generation-run record.
- A user without Program Manage or outside the program scope receives a forbidden result and creates neither events nor generation records.

### EVT-02.4 — Recovery and concurrency are safe

- Exercise retry after a generation run has recorded a partial/failed unit. Retry resumes from durable run state and never duplicates already-created events.
- Start two equivalent generation attempts concurrently. At most one event exists for each unique program/start pair; both responses expose deterministic created/skipped or conflict outcomes.
- A malformed/ambiguous preview is rejected before event writes and leaves the event directory unchanged.

### EVT-02.5 — Reachability and accessibility

- All controls above are reachable from the current Program Workspace in the browser without navigating through the legacy manager.
- Loading, empty, error, stale, forbidden, and success states are visible in the DOM with accessible names and keyboard operation.
- The shared event directory and event detail remain usable at narrow viewport widths.

## Required evidence

- Focused worker/store/component tests for the criteria above.
- Local Playwright trace against `wrangler dev` + local D1, with each criterion asserted through DOM or response state.
- Evidence paths recorded in this file after verification; no Cloudflare account or Apps Script/Google Sheet mutation.

## Implementation evidence (committed with #252)

Commit: `HEAD` of `feat/recurring-event-generation-252` (see `git log`), based on
`origin/main` `b7f1842741d61b7f15bf4b8809a3e69a2fe9e1d0` + the trace commit.

### Focused verification commands (all passed locally)

| Command | Result |
| --- | --- |
| `pnpm --dir web typecheck` | pass (Next app + worker tsconfigs) |
| `pnpm --dir web exec vitest run lib/programs/programs.test.ts` | 120/120 passed |
| `pnpm --dir web exec vitest run --config vitest.components.config.ts` | 305/305 passed (27 files) |
| `pnpm --dir web exec vitest run worker.test.ts worker.auth.test.ts` | 75/75 passed |
| `pnpm typecheck` (root, includes `tests/e2e/tsconfig.json`) | pass |

### Criterion → evidence mapping

- **EVT-02.1 Preview is exact and non-mutating** — worker: `EVT-02.1 preview materializes exact weekly/monthly occurrences with locations and exceptions, without writing events` (asserts weekly + monthly rows, HK wall times, rule locations on every row, CANCEL skip rows, RESCHEDULE overrides, deterministic `plan_id:rule_id:date` occurrence identity, ascending ordering, repeat preview returns same plan identity + ordering, zero events written, no `EVENT_GENERATE`/`EVENT_CREATE` audits, `EVENT_PREVIEW` SUCCESS audit) and `EVT-02.1 rule locations flow into generated events`; zero rules → `preview on a program with no schedule rules returns 422 with a FAILED audit row`. Component DOM: `EVT-02 recurring preview and generation UI` → plan rows, location, 本次已取消 badge, plan identity, generate control all reachable in the events task.
- **EVT-02.2 Plan identity prevents stale writes** — worker: `EVT-02.2 generation with a stale plan is rejected before writes` (rule PATCH → old plan rejected 409 `STALE_PLAN`, event count unchanged, `EVENT_GENERATE` FAILED audit, changed rules produce a new plan identity, fresh plan generates 200).
- **EVT-02.3 Generation authorized, idempotent, auditable** — worker: `EVT-02.3 generation is idempotent, deterministic, and audited` (repeat reuses the same `run_id`, created 0/skipped 2, unique `(program, starts_at)`, durable `program_generation_runs` row, `EVENT_GENERATE` SUCCESS audit), `EVT-02.3 a member without Program Manage is forbidden and writes nothing` (403 `FORBIDDEN`, zero events, zero run rows), `EVT-02.3 CANCEL exceptions are skipped and RESCHEDULE moves the generated event` (skipped=1 with durable `CANCEL` detail, no event on the CANCEL date, RESCHEDULE wall time applied).
- **EVT-02.4 Recovery and concurrency are safe** — worker: `EVT-02.4 generation resumes from a durable partial run without duplicating events` (pre-seeded partial run with one `failed` item → retry resumes same run, retries the failed unit, completed, no duplicates), `EVT-02.4 two concurrent generation attempts create at most one event per start` (Promise.all → one run, unique starts, counts sum to occurrences), `EVT-02.4 malformed or unknown plans are rejected before writes` (404 `PLAN_NOT_FOUND`, 422 missing `plan_id`, zero events/runs).
- **EVT-02.5 Reachability and accessibility** — component DOM tests (jsdom): preview controls reachable only for Recurring + manage, loading/empty/error/stale states asserted through accessible names and `role="alert"` output, generate disabled until a current plan exists, stale-plan error clears the plan and requires a new preview; full component suite 305/305 keeps the shared event directory/detail links green.

### Coordinator READY verification (local gate, per `tests/e2e/README.md`)

```sh
# terminal 1
pnpm dev:local                                   # build + migrate + wrangler dev on 127.0.0.1:8787
# terminal 2
pnpm db:seed:local                               # E2E_ disposable accounts
pnpm db:seed:demo                                # E2E_DEMO_ department/programs/events (now preview→generate)
pnpm exec playwright test -c tests/e2e/programs-d1.config.ts
pnpm exec playwright test -c tests/e2e/attendance-d1.config.ts
```

Browser evidence paths (after a gate run): `tests/e2e/test-results/programs-d1-results.json`,
`tests/e2e/test-results/attendance-d1-results.json`, plus the Playwright trace archives in
`tests/e2e/test-results/`. No Cloudflare account or Apps Script/Google Sheet mutation: the seed
scripts refuse non-loopback targets and the gate runs entirely against local D1 `E2E_`/`E2E_DEMO_`
fixtures.
