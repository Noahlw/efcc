# ENR-01 — Enrollment operations acceptance trace

## Scope

- Ticket: #253
- Base: `origin/main` at `b7f1842741d61b7f15bf4b8809a3e69a2fe9e1d0`
- Public surface: the reachable Programs management workspace and participant request flow. Do not extend the unreferenced legacy `programs-manager` / `programs-enrollment-panel` path.
- Local gate: `wrangler dev` at `127.0.0.1:8787` with disposable `E2E_`/`E2E_DEMO_` D1 fixtures only.

## Observable setup

1. Start the local app with `pnpm dev:local`.
2. Seed the disposable fixtures with the checked-in local seed commands.
3. Use a participant fixture to submit a request for a target program.
4. Use a scoped Manager fixture with Program Manage capability to open Management → the target program → Participants.

## Acceptance criteria

### ENR-01.1 — Queue states and counts are truthful

- The reachable Participants task exposes distinct Pending, Active, and historical/withdrawn views (or equivalent accessible filters/tabs).
- Counts are derived from the current scoped records and match the visible rows; pending is not conflated with active or historical withdrawal.
- Loading, empty, forbidden, stale, conflict, and server-error states are visible in the DOM and do not display fabricated success or stale counts after refresh.

### ENR-01.2 — Request decisions are atomic

- Approve a pending request inline. The response and refreshed DOM show the request decision and the corresponding active enrollment together, or neither when the operation fails.
- Reject a pending request inline. It leaves no active enrollment and appears in the historical decision view with the recorded reason/outcome.
- Attempt a second decision, decide a withdrawn/stale request, or use a request from another scope. The operation returns a deterministic conflict/forbidden/stale result and preserves the queue, active enrollment, and counts.
- Concurrent approvals of one request yield at most one active enrollment and one terminal request decision; the loser gets an explicit conflict/idempotent result.

### ENR-01.3 — Assisted enrollment enforces eligibility and scope

- A ManagerOnly operator can enroll an eligible Active Account in the current scoped program from the reachable Participants task.
- An inactive/nonexistent account, duplicate active enrollment, out-of-scope program/account, or non-ManagerOnly user is rejected before mutation with an explicit error.
- Successful assisted enrollment creates the active enrollment and its audit record; failed/conflicting attempts do not create a partial row and preserve current counts.

### ENR-01.4 — Audit and authorization are server-owned

- Worker/D1 responses and database state are the source of truth for decisions, assisted enrollment, counts, and audit outcomes; client-only state cannot claim success.
- Capability checks are applied on every mutation and scope is revalidated at mutation time, not only when the queue was loaded.
- No Apps Script or Google Sheet is mutated.

### ENR-01.5 — Reachability and accessibility

- A participant can submit a request in the browser and a manager can find and act on it without navigating through the legacy manager.
- All row actions have accessible names, keyboard operation, visible pending/error/success states, and work at narrow viewport widths.

## Required evidence

- Focused worker/store/component tests for approval atomicity, rejection/history, assisted eligibility, scope, duplicate/race, and stale/error outcomes.
- Local Playwright trace against `wrangler dev` + local D1, with each criterion asserted through DOM or response state.
- Evidence paths recorded in this file after verification; automated tests may reset only disposable `E2E_`/`E2E_DEMO_` fixtures.

## Implementation evidence

- `web/lib/programs/programs.test.ts`: `cd web && pnpm test -- lib/programs/programs.test.ts` passed (Vitest configuration ran 16 files / 334 tests). Coverage includes one-batch enrollment snapshots, atomic `{request, enrollment}` approval/rejection responses, request-version audit snapshots, decision races, expected-version stale rejection, terminal conflict, selected-program active-member eligibility, assisted same-actor quiet duplicates/cross-actor conflicts, and audit outcomes.
- `web/lib/programs/program-workspace.test.tsx`: `cd web && pnpm test:components -- lib/programs/program-workspace.test.tsx` passed (component configuration ran 27 files / 306 tests). Coverage asserts the reachable Participants snapshot queue tabs, inline controls, no misleading `aria-controls` references, assisted ManagerOnly enrollment, request-version submission, visible stale-row error, and queue preservation after duplicate assisted enrollment.
- `tests/e2e/programs-d1.test.ts`: added local-browser coverage for the reachable manager Participants queue (server-shaped counts, pending inline approval, active result) and member direct Participants management denial; coordinator must run the local `wrangler dev` READY gate.
- `web`: `pnpm typecheck` passed (`tsconfig.json` and `tsconfig.worker.json`).
- Reachable UI implementation: `web/lib/programs/program-workspace.tsx` (`ProgramWorkspace` → `WorkspaceTask` → `ParticipantsTask`); legacy enrollment panels were not changed.
- Local Playwright/`wrangler dev` READY evidence remains coordinator-owned and must be appended here after the disposable `E2E_`/`E2E_DEMO_` gate runs.

## Coordinator READY Programs results

- Generated: 2026-08-13T07:01:28.138Z
- Target: http://127.0.0.1:8787/
- Total assertions: 66 | Passed: 66 | Failed: 0

| Role | Assertion | Result | Detail |
|------|-----------|--------|--------|
| phone | admin enters Participant mode with capability-shaped Management entry | PASS |  |
| phone | staff also enters Participant mode before any management action | PASS |  |
| phone | member enters Participant mode without a management gateway | PASS |  |
| phone | mode switching preserves a valid Program intent and exposes tabpanel semantics | PASS |  |
| phone | malformed direct intent stays recoverable inside Programs | PASS |  |
| phone | restores a direct Programs intent after session expiry and login | PASS |  |
| desktop | admin enters Participant mode with capability-shaped Management entry | PASS |  |
| desktop | staff also enters Participant mode before any management action | PASS |  |
| desktop | member enters Participant mode without a management gateway | PASS |  |
| desktop | mode switching preserves a valid Program intent and exposes tabpanel semantics | PASS |  |
| desktop | malformed direct intent stays recoverable inside Programs | PASS |  |
| desktop | restores a direct Programs intent after session expiry and login | PASS |  |
| phone | member sees Listed catalog rows and never the Unlisted fixture | PASS |  |
| phone | admin sees the Unlisted fixture through scoped management access | PASS |  |
| phone | search narrows the catalog and clearing restores the same rows | PASS |  |
| phone | empty search result is recoverable by clearing | PASS |  |
| phone | row selection hands off through the canonical Program intent URL | PASS |  |
| desktop | member sees Listed catalog rows and never the Unlisted fixture | PASS |  |
| desktop | admin sees the Unlisted fixture through scoped management access | PASS |  |
| desktop | search narrows the catalog and clearing restores the same rows | PASS |  |
| desktop | empty search result is recoverable by clearing | PASS |  |
| desktop | row selection hands off through the canonical Program intent URL | PASS |  |
| phone | direct detail survives refresh and returns to the directory | PASS |  |
| phone | member receives privacy-preserving unavailable state for Unlisted detail | PASS |  |
| desktop | direct detail survives refresh and returns to the directory | PASS |  |
| desktop | member receives privacy-preserving unavailable state for Unlisted detail | PASS |  |
| phone | member can submit one request, see Pending, and withdraw it | PASS |  |
| phone | ManagerOnly detail explains that participants cannot self-enroll | PASS |  |
| desktop | member can submit one request, see Pending, and withdraw it | PASS |  |
| desktop | ManagerOnly detail explains that participants cannot self-enroll | PASS |  |
| phone | admin searches the scoped Directory and navigates focused Workspace tasks | PASS |  |
| phone | manager Participants queue shows scoped counts and approves a pending request | PASS |  |
| phone | keeps Directory and Workspace entry points keyboard-operable | PASS |  |
| phone | member direct management links stay out of scope | PASS |  |
| phone | staff uses the same capability-shaped Directory information architecture | PASS |  |
| phone | leader exact scope and manager inheritance stay distinct | PASS |  |
| phone | a revoked or unknown direct management link stays generic | PASS |  |
| desktop | admin searches the scoped Directory and navigates focused Workspace tasks | PASS |  |
| desktop | manager Participants queue shows scoped counts and approves a pending request | PASS |  |
| desktop | keeps Directory and Workspace entry points keyboard-operable | PASS |  |
| desktop | member direct management links stay out of scope | PASS |  |
| desktop | staff uses the same capability-shaped Directory information architecture | PASS |  |
| desktop | leader exact scope and manager inheritance stay distinct | PASS |  |
| desktop | a revoked or unknown direct management link stays generic | PASS |  |
| phone | renders all scope-owned groups and omits recurring controls for OneOff | PASS |  |
| phone | renders unavailable copy for Schedule and Attendance when their modules are disabled | PASS |  |
| phone | consequential discoverability change requires confirmation before it saves | PASS |  |
| desktop | renders all scope-owned groups and omits recurring controls for OneOff | PASS |  |
| desktop | renders unavailable copy for Schedule and Attendance when their modules are disabled | PASS |  |
| desktop | consequential discoverability change requires confirmation before it saves | PASS |  |
| phone | Staff denies self-assignment, revokes the seeded leader, and re-grants it | PASS |  |
| desktop | Staff denies self-assignment, revokes the seeded leader, and re-grants it | PASS |  |
| phone | Admin grants a Department Manager, scope inherits, then revokes | PASS |  |
| desktop | Admin grants a Department Manager, scope inherits, then revokes | PASS |  |
| phone | creates a OneOff, operates multiple Events, edits, and blocks archive | PASS |  |
| phone | member direct Program mutation is denied server-side | PASS |  |
| desktop | creates a OneOff, operates multiple Events, edits, and blocks archive | PASS |  |
| desktop | member direct Program mutation is denied server-side | PASS |  |
| phone | admin creates, deep-links, and edits an event with HK wall display | PASS |  |
| phone | safe deactivation is immediate with Undo; cancellation retires controls | PASS |  |
| phone | an active Program enrollment alone does not gate this event's deactivation | PASS |  |
| phone | a currently open check-in window with zero check-ins still requires confirmation to deactivate | PASS |  |
| desktop | admin creates, deep-links, and edits an event with HK wall display | PASS |  |
| desktop | safe deactivation is immediate with Undo; cancellation retires controls | PASS |  |
| desktop | an active Program enrollment alone does not gate this event's deactivation | PASS |  |
| desktop | a currently open check-in window with zero check-ins still requires confirmation to deactivate | PASS |  |

